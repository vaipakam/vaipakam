# Review Findings - 2026-05-01

## Instructions

- Look into the Findings with unticked checkbox and need to work on it
- Always ask if there is a better apporach and if anything need to be discussed
- Once completed / resolved the Findings, tick the checkbox
- The new findings need to be updated at the top with running ID

---

- [ ] **00030: Low: Borrower LIF VPFI custody pays the matcher kickback on forfeiture; spec §6b says "all forfeited to Treasury".**

- TokenomicsTechSpec.md §6b "Settlement" (line 372-373) reads "On default or HF-based liquidation, the borrower rebate is `0` and **all VPFI held for that loan is forfeited to Treasury**." `LibVPFIDiscount.sol:495-503` (`forfeitBorrowerLif`) instead splits the held VPFI into a 1% matcher kickback + 99% to treasury whenever the loan was matched. Spec §5a (line 247) DOES authorize the matcher kickback "on proper settlement or forfeiture" — so the two §-references contradict each other internally. Either pick one wording or amend §6b to "the held VPFI flushes net of the matcher share to Treasury". Small economic divergence on a corner-case path; flagged because the spec is self-contradictory.

---

- [ ] **00029: Low: Borrower VPFI LIF path silently rejects sub-tier-1 escrows even when escrow > full LIF.**

- TokenomicsTechSpec.md §6b "Acceptance-time flow" (lines 357-365) lists three eligibility predicates: lending asset is liquid, borrower has the platform-level consent, and "the borrower's escrow on that same lending chain must contain enough VPFI to cover the full, non-discounted `0.1%` `Loan Initiation Fee` equivalent." `LibVPFIDiscount.sol:261` adds a `tier == 0` short-circuit (returns `(false, 0, 0)` from `quote`), so a borrower with any escrow balance below tier-1 silently falls through to the lending-asset LIF path even when the LIF is well under their balance. The implementation comment justifies this as "tier-0 users earn no time-weighted rebate" (true), but the spec doesn't authorize the gate. Either document the tier-0 carve-out in TokenomicsTechSpec §6b or drop the check.

---

- [ ] **00028: Medium: Treasury Recycling Rule (38/38/24) has no on-chain implementation.**

- TokenomicsTechSpec.md §9 (lines 528-534) AND ProjectDetailsREADME.md §10 "VPFI Received From Protocol-Fee Flows" (lines 1120-1124) prescribe an automated 38% ETH / 38% wBTC / 24% retain split for VPFI received as fees, with conversions "through the configured on-chain swap-aggregator proxy". `TreasuryFacet.sol` (only 145 lines) exposes `claimTreasuryFees`, `getTreasuryBalance`, `mintVPFI` — no recycling routing, no `recycleVPFI`/`buyback` selector, no swap-aggregator hook for the treasury surplus path. Same gap applies to the 2% Insurance-pool surplus auto-recycle rule. If recycling is intended to remain a manual multisig op, the spec should say so; otherwise this is a Phase-1 functional gap.

---

- [ ] **00027: Low: `StakingAprSet` (and sibling governance events) omits the took-effect timestamp the spec calls out as authoritative.**

- TokenomicsTechSpec.md §7a "Governance audit trail" (line 470-471) states every governance APR change should emit an event recording the new APR **and the timestamp when it took effect**; that event stream is the authoritative historical audit trail. `ConfigFacet.sol:51` declares `event StakingAprSet(uint16 aprBps)` — no timestamp parameter. `block.timestamp` is recoverable from the log envelope off-chain, but the spec is explicit that the timestamp is part of the emitted record. Add `uint64 effectiveAt` (and similarly to the `Range*EnabledSet` / `PartialFillEnabledSet` siblings if you want consistent governance-event semantics).

---

- [ ] **00026: Low: `claimInteractionRewards` signature drifted from spec — no `dayId[]` argument; on a different facet than the spec lists.**

- TokenomicsTechSpec.md §4a "Diamond surface" (line 220) declares `ClaimFacet.claimInteractionRewards(dayId[])`. Actual entry point at `InteractionRewardsFacet.sol:92` is `claimInteractionRewards()` (no arg) on `InteractionRewardsFacet`, not `ClaimFacet` — it walks an internal day window automatically. Same paragraph names a `chainInterestUSD(dayId)` view that doesn't appear with that exact name on `RewardReporterFacet` (the public read is shaped differently). Update the spec to match the implemented "auto-walk" model + correct facet name, or vice-versa.

---

- [x] **00025: Medium: ERC-20 loan duration has no on-chain upper bound; spec mandates 1–365 days.**

- ProjectDetailsREADME.md §2 (line 221) says "Durations: Configurable from 1 day to 1 year." WebsiteReadme.md line 470 reinforces "the loan-duration input must be restricted to the supported protocol range of `1` to `365` days". `OfferFacet.sol:349` only enforces `params.durationDays == 0`; nothing in OfferFacet, LibVaipakam, or LibLifecycle caps `durationDays` at 365. A misclick or malicious offer can post a 1000-day loan that grace-period logic still resolves under the `>= 180 → 2 weeks` tier, AND `interest = principal × rate × days / 365` over-charges past 365 days. Frontend validation alone is the only gate today.
- **Resolved 2026-05-01**: governance-tunable cap shipped. (1) New `ProtocolConfig.maxOfferDurationDays` storage slot + library default `MAX_OFFER_DURATION_DAYS_DEFAULT = 365` + bounded floor / ceil constants (`MIN_OFFER_DURATION_DAYS_FLOOR = 7`, `MAX_OFFER_DURATION_DAYS_CEIL = 1825` ≡ 5 years). Floor prevents an accidental "1 day max" lockout; ceil caps the interest formula's accuracy degradation. (2) `LibVaipakam.cfgMaxOfferDurationDays()` accessor with the standard zero-sentinel-falls-back-to-default pattern. (3) `OfferFacet._createOfferSetup` now reverts with `OfferDurationExceedsCap(provided, cap)` when `params.durationDays > cfgMaxOfferDurationDays()`. (4) `ConfigFacet.setMaxOfferDurationDays(uint16)` admin-gated setter (`onlyRole(ADMIN_ROLE)`) bounded inside [floor, ceil]; emits `MaxOfferDurationDaysSet`. (5) Selector wired into `_getConfigSelectors()` in DeployDiamond + `getConfigFacetSelectors()` in HelperTest. (6) `getProtocolConfigBundle` extended to expose `maxOfferDurationDays` so the frontend can read the live cap. (7) Regression test `testCreateOfferRevertsIfDurationAboveCap` in `OfferFacetTest.t.sol` asserts revert at 366 days. ConfigFacetTest's bundle test extended to cover the new field. Forge: 1391/0/5 (skip), up from 1390. Frontend `useProtocolConfig` hook + offer-creation duration input still need a follow-up to consume the live value (currently uses the hard-coded 365); that's a small frontend tweak, not a contract finding.

---

- [x] **00024: Medium: `getAssetRiskProfile` return shape diverges from the spec's documented contract surface.**

- ProjectDetailsREADME.md §13 (lines 1419-1425) declares `getAssetRiskProfile(address) returns (uint256 ltvBps, uint256 liquidationThresholdBps, uint256 currentPriceUSD, bool isLiquid, bool isSupported)`. `OracleFacet.sol:611-620` returns `(bool isSupported, LibVaipakam.LiquidityStatus status, uint256 maxLtvBps, uint256 liqThresholdBps, uint256 liqBonusBps)`. Field order is reversed, `currentPriceUSD` is missing entirely, the `bool isLiquid` is replaced by an enum, and `liqBonusBps` is an extra field the spec doesn't list. External integrators (DefiLlama / portfolio apps the spec explicitly courts) will silently mis-decode the tuple.
- **Resolved 2026-05-01 by spec update**: ProjectDetailsREADME.md §13 (lines 1426-1432) now declares `getAssetRiskProfile` returning `(bool isSupported, uint8 status, uint256 maxLtvBps, uint256 liqThresholdBps, uint256 liqBonusBps)` — exactly the shape `OracleFacet.sol:611-620` returns. Line 1438 explicitly notes the helper does not return a USD price ("consumers that need pricing should use the dedicated oracle / price read surface instead"). Code matches; no integrator-facing change required.

---

- [x] **00023: Low: VPFI on-chain token name is "Vaipakam DeFi Token" but TokenomicsTechSpec mandates "Vaipakam Finance Token".**

- TokenomicsTechSpec.md:5 says **"Token Name: Vaipakam Finance Token"** and the parameter table at line 40 repeats it. Both `VPFIToken.sol:101` (`__ERC20_init("Vaipakam DeFi Token", "VPFI")`) and `VPFIMirror.sol:68` (`__OFT_init("Vaipakam DeFi Token", "VPFI", owner_)`) initialize with the older name. ProjectDetailsREADME §10.2 lists "Vaipakam DeFi Token" so the two specs disagree internally; the deployed bytecode follows ProjectDetails. Decide which name is canonical, then either correct the spec or rename the token (the latter requires a redeploy because OZ ERC20 name is initializer-set, not mutable).
- **Resolved 2026-05-01 by spec update**: TokenomicsTechSpec.md §1 line 5 and §2 parameter table line 40 now read "Vaipakam DeFi Token", matching ProjectDetailsREADME.md §10.2 and the on-chain `VPFIToken.__ERC20_init` / `VPFIMirror.__OFT_init` initializer arguments. Internal spec contradiction is gone; deployed bytecode is canonical; no rename / redeploy needed.

---

- [ ] **00022: Medium: ConfigureLZConfig applies local-chain confirmations to the receive library too.**

- `ConfigureLZConfig.s.sol:207-214` builds the receive-side `UlnConfig` from `_confirmationsFor(chainId_)` — the LOCAL chain's confirmation budget. The script's own comment acknowledges this is asymmetric vs. peer-confirmation semantics ("Adjust if a per-peer inbound confirmation matrix is ever needed"). For a cross-chain mesh this means inbound packets are accepted using the local chain's confirmation budget, not the source chain's. Contradicts the documented Cross-Chain Security Policy in `CLAUDE.md` which lists per-chain confirmations as source-side budgets. Real-world impact: a destination chain configured with low confirmations (e.g. Base 10) will accept packets from Ethereum (15) after only 10 source-chain confirmations, weakening the policy at the receive boundary.

---

- [ ] **00021: Low: Sepolia\* / BaseSepoliaPartialFlows seed scripts torch prior state every run.**

- `SepoliaActiveLoan.s.sol:65-98` (and the three sister `Sepolia*` / `BaseSepoliaPartialFlows.s.sol` scripts) deploy fresh `ERC20Mock`s + a fresh `MockChainlinkRegistry` and call `OracleAdminFacet.setChainlinkRegistry` on every run. Any pre-existing offers/loans whose collateral or lending asset was the previous registry's tokens immediately becomes Illiquid post-rerun. Re-running the seeder torches the dashboard data the previous run created. Idempotency is silently false; flagged in the script comments as "append-only" but the registry repointing breaks that.

---

- [ ] **00020: Medium: MigrateOAppGovernance silently skips the VPFI mirror unless an env var is also set.**

- `MigrateOAppGovernance.s.sol:101-104` reads the mirror address only from `<CHAIN>_VPFI_MIRROR_ADDRESS` env (`vm.envOr(..., address(0))`); never falls through to the persisted `Deployments.readVPFIMirror()` artifact. `DeployVPFIMirror` writes the mirror under the `vpfiMirror` JSON key but the migration script doesn't read it back. Result: an operator running `MigrateOAppGovernance` with only the standard env vars (no `<CHAIN>_VPFI_MIRROR_ADDRESS` exported) silently skips the mirror migration, leaving the mirror's owner / endpoint config under the deployer instead of the timelock.

---

- [ ] **00019: Low: VPFI\* deploy scripts don't assert `owner != address(0)`.**

- `DeployVPFIBuyAdapter.s.sol:96-110` and the sister `DeployVPFIBuyReceiver.s.sol` / `DeployVPFICanonical.s.sol` / `DeployVPFIMirror.s.sol` / `DeployRewardOAppCreate2.s.sol` all deploy a UUPS proxy and pass `owner = vm.envAddress("VPFI_OWNER")` (or equivalent) into the initializer. `vm.envAddress` reverts on a missing env var (good), but a deliberate `0x0` literal in `.env` would not — the proxy would deploy with `owner = address(0)` and no `setGuardian` recovery path. One-line fix: assert non-zero before broadcast.

---

- [ ] **00018: Medium: ConfigureOracle requires `broadcaster == IERC173.owner()`, blocking post-timelock runs.**

- `ConfigureOracle.s.sol:118-127` pre-flight check requires `broadcaster == IERC173(diamond).owner()`. Post-`TransferAdminToTimelock`, the Diamond owner is the timelock contract — no EOA. Most setters in the script (`setallowanceTarget`, `setZeroExProxy`) only need `ADMIN_ROLE`, not ownership, so the gate is over-strict. Owner-gated setters need a timelock-scheduled call regardless. Result: the script becomes un-runnable directly under the documented governance path.

---

- [ ] **00017: Medium: SetInteractionLaunch uses `PRIVATE_KEY` instead of `ADMIN_PRIVATE_KEY`.**

- `SetInteractionLaunch.s.sol:34` reads `PRIVATE_KEY`. The setters it calls (`setInteractionLaunchTimestamp`, `setInteractionCapVpfiPerEth`) are ADMIN_ROLE-gated. The runbook documents separate deployer / admin EOAs (deployer renounces all roles after handover); operators following that flow will hit a revert. Sibling configure-scripts (`ConfigureOracle`, `ConfigureVPFIBuy`, `ConfigureNFTImageURIs`) consistently read `ADMIN_PRIVATE_KEY` for admin-role calls; this one is the outlier.

---

- [ ] **00016: Medium: ConfigureRewardReporter uses `PRIVATE_KEY` instead of `ADMIN_PRIVATE_KEY`.**

- Same root cause as 00017. `ConfigureRewardReporter.s.sol:47` reads `PRIVATE_KEY` but calls ADMIN_ROLE-gated setters (`setLocalEid`, `setBaseEid`, `setRewardOApp`, `setIsCanonicalRewardChain`, `setExpectedSourceEids`). Reverts post-handover when run with the documented deployer/admin split.

---

- [ ] **00015: Medium: AddOracleAdmin is stale — uses `PRIVATE_KEY` for diamondCut, AND its premise no longer holds.**

- `AddOracleAdmin.s.sol:19,21` reads `PRIVATE_KEY` and broadcasts a `diamondCut` with it. Post-handover the deployer EOA holds zero roles + zero ownership, so the call reverts with `NotContractOwner`. Additionally the script's premise ("DeployDiamond didn't register OracleAdminFacet") is no longer true — `DeployDiamond` already cuts OracleAdminFacet's 20 selectors at line 131, so even if the auth issue were fixed the script reverts with `CannotAddSelectorThatAlreadyExists`. Either delete the script or rewrite as a `Replace`-cut sourced from `ADMIN_PRIVATE_KEY`.

---

- [x] **00014: High: UpgradeOracle uses `PRIVATE_KEY` for diamondCut → reverts post-handover.**

- `UpgradeOracle.s.sol:48,110` reads `PRIVATE_KEY` and broadcasts the `IDiamondCut.diamondCut(...)` call with it. Post-handover the deployer holds zero roles, so the cut reverts with `NotContractOwner`. The sister script `UpgradeOracleFacet.s.sol:34-62` correctly splits PRIVATE_KEY (deploy) and ADMIN_PRIVATE_KEY (cut) — `UpgradeOracle.s.sol` is the older / broken variant. Either delete it (use `UpgradeOracleFacet` instead) or fix the broadcaster split.
- **Resolved 2026-05-01**: `UpgradeOracle.s.sol` deleted. The working sister `UpgradeOracleFacet.s.sol` already covers the OracleFacet upgrade path with the correct deployer/admin key split. The mock-Chainlink wiring that `UpgradeOracle.s.sol` also did is already covered by `DeployTestnetLiquidityMocks.s.sol` (called as part of the bootstrap sequence), so no functionality is lost.

---

- [x] **00013: High: UpgradeOracle stale OracleFacet selector list (4 of 9) splits Diamond bytecode after Replace.**

- `UpgradeOracle.s.sol:82-87` lists 4 OracleFacet selectors for the Replace cut; the live OracleFacet has 9 (`getAssetPrice`, `getETHPrice`, `getAssetPriceForChain`, `getETHPriceForChain`, `checkLiquidity`, `sequencerHealthy`, `getSequencerUptimeFeed`, `isAssetSupported`, `getIlliquidAssets`, `getAssetRiskProfile`). `Replace` only updates the listed 4 — the other 5 keep pointing at the prior OracleFacet bytecode. Same shape applies to OracleAdminFacet (script lines 95-102 lists 7 selectors; canonical has 20). Lines 104-108 try `Add` for those 7 selectors — if any already exist on the Diamond, diamondCut reverts with `CannotAddSelectorThatAlreadyExists`. Real-world impact: running this script on a current-shape Diamond produces a Diamond split across two OracleFacet bytecode versions — read paths inconsistent.
- **Resolved 2026-05-01**: same fix as 00014 — script deleted. Sister `UpgradeOracleFacet.s.sol` consumes the OracleFacet upgrade path correctly (uses `getOracleFacetSelectors()` from `HelperTest`, so all 10 selectors are Replace-cut atomically — no drift). 00011 (RedeployFacets ProfileFacet drift) and 00012 (ReplaceStaleFacets multi-facet drift) remain as a separate batch — same root cause (parallel selector lists without shared truth), but a principled fix needs a separate design decision before code lands.

---

- [ ] **00012: High: ReplaceStaleFacets stale selector lists for OfferFacet (6/10) + OracleFacet (4/9) + EscrowFactoryFacet (18/22).**

- `ReplaceStaleFacets.s.sol:72-80` lists 6 OfferFacet selectors (`createOffer`, `acceptOffer`, `cancelOffer`, `getCompatibleOffers`, `getUserEscrow`, `getOffer`). The current canonical OfferFacet has 10 selectors — adds `getOfferDetails`, `createOfferWithPermit`, `acceptOfferWithPermit`, **`acceptOfferInternal`** (the cross-facet entry that `OfferMatchFacet.matchOffers` calls). After this script's Replace, the Diamond points 6 selectors at the new bytecode and 4 selectors at the prior bytecode — most damaging: if the new and old OfferFacets diverge on storage layout or the cross-facet contract on `acceptOfferInternal`, `matchOffers` calls land on inconsistent bytecode. Same shape for OracleFacet (4 of 9) and EscrowFactoryFacet (18 of 22). Real-world impact: a "redeploy a stale facet" cut produces a Diamond whose loupe reports the new facet but whose un-listed selectors still execute the old one.

---

- [ ] **00011: High: RedeployFacets ProfileFacet selector list drifted (15 of 25 cut).**

- `RedeployFacets.s.sol:121-138` lists 15 ProfileFacet selectors. The canonical `DeployDiamond._getProfileSelectors():434-466` lists 25 — adds Phase 6 keeper selectors (s[17]-s[21]) plus sanctions (s[22]-s[24]) plus `setLoanKeeperEnabled`/`isApprovedKeeper`. Replace-cutting only 15 leaves 10 selectors pointed at the OLD ProfileFacet bytecode, splitting the per-user keeper / sanctions state across two facet implementations. Calls like `setOfferKeeperEnabled`, `setKeeperActions`, `setSanctionsOracle`, `isSanctionedAddress` go to the stale facet. Same drift hazard could land elsewhere as the Diamond grows; root cause is two parallel selector lists for the same facet without a shared source of truth.

---

- [x] **00010: High: WATCHER_ROLE is never granted on a fresh DeployDiamond run — auto-pause primitive unreachable post-deploy.**

- `LibAccessControl.WATCHER_ROLE` (LibAccessControl.sol:38) gates `AdminFacet.autoPause`. `AccessControlFacet.initializeAccessControl()` only grants seven roles to the initial owner; WATCHER_ROLE is omitted there. The `roles[7]` array at `DeployDiamond.s.sol:186-194` mirrors the same seven and so the hand-over loop at lines 199-216 never grants WATCHER_ROLE to admin nor revokes it from deployer. After a fresh deploy, WATCHER_ROLE is unowned (`hasRole(WATCHER_ROLE, *)` returns false for everyone). The auto-pause primitive — the always-armed safety net documented as a 30-min governance-tunable kill switch — is unreachable until governance grants the role explicitly. Worse: there's no symbolic check between `LibAccessControl`'s role list and the deploy script's array, so this drift can recur silently when a future role is added.
- **Resolved 2026-05-01**: three-part fix. (1) `LibAccessControl.initializeAccessControl()` now grants `WATCHER_ROLE` to the initial owner + sets its admin to `DEFAULT_ADMIN_ROLE`, alongside the existing seven roles. (2) New `LibAccessControl.grantableRoles()` view exposes the canonical role list as the single source of truth; `DeployDiamond.s.sol` consumes that view directly so its handover loop and the library's init grants can no longer drift. Adding a new role now means just adding it to `grantableRoles()` and `initializeAccessControl()` — the deploy script picks it up automatically. (3) `DeployerZeroRolesTest.t.sol` extended with two regression tests: `testRoleListsParity` (asserts library list ↔ test ALL_ROLES match byte-for-byte) and `testEveryGrantableRoleGrantedAtInit` (asserts every grantable role is held by the initial owner after init — would have caught the original bug). Forge suite: 1390 / 0 / 5 (skip), up from 1388 with the two new tests. **WATCHER_ROLE scope confirmed narrow**: only gates `AdminFacet.autoPause(string)` — a time-bounded pause (default 30 min, max 2 hours) that's a no-op when already paused. Cannot unpause (PAUSER_ROLE retains that). A compromised watcher's worst case is a 2-hour freeze.

---

- [ ] **00009: Medium: Frontend test suite cannot run — `@testing-library/dom` peer dep missing.**

- All 44 test files in `frontend/test/**` fail at module resolution with `Error: Cannot find module '@testing-library/dom'` (required by `@testing-library/react@16.3.2`). The peer dep is not declared in `frontend/package.json` and is not installed under `frontend/node_modules/@testing-library/`. Adjacent issue: `package.json` declares `"engines": { "node": ">=20.19.0" }` but a Node 18.x installation hits a separate vitest/rolldown startup error (`styleText` not exported from `node:util`). Fix: add `"@testing-library/dom": "^10.4.x"` to devDependencies + reinstall, AND ensure the development / CI Node version is ≥ 20.19. Without this, `npm test` returns 44 file-level failures with zero tests actually run, so the suite provides no regression coverage today.

- [ ] **00008: High: Fixed-rate VPFI buy still relies on a pre-funded Diamond sale reserve.**

- TokenomicsTechSpec.md says the fixed-rate purchase program must not rely on a pre-minted sale reserve. VPFIDiscountFacet.sol still documents that the Diamond sells VPFI from its own balance and that ops must fund it during canonical deploy, then both direct and bridged buys transfer from the Diamond balance and revert with VPFIReserveInsufficient when on-hand VPFI is below the requested amount. This can halt fixed-rate buys even when caps remain available, and it contradicts the documented mint/release-on-receipt model.

---

- [ ] **00007: High: Cross-chain interaction reward source configuration omits Base from the global denominator.**

- TokenomicsTechSpec.md defines interaction rewards using a protocol-wide daily denominator across all chains, with Base acting as the canonical aggregator. ConfigureRewardReporter.s.sol describes REWARD_EXPECTED_SOURCE_EIDS as mirror EIDs only, and BaseSepoliaDeploy.md sets it to 40161,40231,40232,40267. RewardAggregatorFacet.\_finalizeAndWrite only sums the configured expectedSourceEids, so Base local closeDay totals are not included unless Base's own localEid is also configured. This excludes Base interest from dailyGlobalInterestRepaid and can distort every chain's reward allocation.

---

- [ ] **00006: High: Mirror interaction reward budgets are not bridged during finalization.**

- TokenomicsTechSpec.md says the interaction reward pool is held on Base, Base computes each mirror chain's budget, and the Base treasury bridges that budget to each mirror through OFT before mirror users claim locally. RewardAggregatorFacet.\_finalizeAndWrite only totals reports, stores global values, and broadcasts the result; it does not bridge or escrow per-chain VPFI budgets. InteractionRewardsFacet.claimInteractionRewards then transfers VPFI from the local Diamond balance, so mirror claims can fail from an unfunded local balance even when the Base pool has enough budget.

---

- [ ] **00005: Medium: The service worker can cache same-origin quote responses.**

- WebsiteReadme.md requires production service workers to cache only the static shell and explicitly bypass dynamic RPC, subgraph, /quote/\*, and transaction-preview responses. frontend/public/sw.js bypasses only Vite internals and /api/ for same-origin GET requests, then applies stale-while-revalidate caching to successful same-origin responses. If quote endpoints are deployed under the same origin, stale quote data can be served after market or liquidity conditions change.

---

- [x] **00004: Medium: The PWA Buy VPFI shortcut points to a route that does not exist.**

- WebsiteReadme.md expects the PWA manifest to expose a Buy VPFI shortcut. frontend/public/manifest.json points that shortcut to /app/buy, but App.tsx registers the actual buy route as /app/buy-vpfi. Installed users who launch the shortcut will land on an unmatched route instead of the public VPFI purchase screen.
- **Resolved 2026-05-01**: manifest.json shortcut updated to `/app/buy-vpfi` (the in-app route added when Buy VPFI moved into the app). Adjacent fix: the `My Loans` shortcut also pointed at `/app/loans`, which has no index route either (only `/app/loans/:loanId`). Repointed at `/app` (Dashboard, where the user's loan list lives).

---

- [x] **00003: Medium: The liquidation UI promises fallback submission but disables the submit path.**

- WebsiteReadme.md says liquidation routing should submit the available ranked route list and route failures into the documented fallback flow. LiquidateButton.tsx displays "The loan will route to FallbackPending on submit" when quotes are unavailable, but handleClick exits unless quotes.calls.length is greater than zero and the Liquidate button is disabled unless status is ready. A no-quote loan therefore cannot be submitted from the frontend despite the UI text saying it will trigger fallback.
- **Resolved 2026-05-01 by spec update**: WebsiteReadme.md §Liquidation now scopes fallback submit to *"if one quote source is unavailable, the UI should still submit a ranked try-list **from the remaining sources where possible**"* (lines 154-155) — the zero-quotes submit obligation is no longer stated. The frontend's disabled-on-zero-quotes behavior matches the updated spec. Recommend a follow-up copy tweak on `LiquidateButton.tsx` so the on-screen "will route to FallbackPending on submit" helper text doesn't promise a submit the button doesn't allow — but that's polish, not a spec divergence.

---

- [x] **00002: Low: Defer: The frontend Terms page is not a verbatim mirror of the canonical Terms markdown.**

- WebsiteReadme.md says the Terms page should mirror the source-of-truth text from docs/TermsOfService.md. TermsPage.tsx states that the JSX reproduces the markdown verbatim, but it omits the GovernanceRunbook.md parenthetical from the protocol-changes section and rewrites the on-chain currentTosVersion/content-hash wording. Users may review UI terms that are not byte-for-byte identical to the canonical document being versioned and hashed.

---

- [x] **00001: Low: The public Navbar wallet display does not resolve ENS or Basenames.**

- WebsiteReadme.md calls out ENS and Basenames resolution for wallet-address display in header/profile surfaces. The app already has AddressDisplay.tsx for that behavior, but Navbar.tsx defines its own shortenAddress helper and renders the shortened hex address directly in both mobile and desktop connected-wallet states. Public header wallet display therefore falls short of the documented identity-resolution behavior.
- **Resolved 2026-05-01**: the public Navbar's entire wallet UI (Connect Wallet button + WalletMenu pill + chain picker + wrong-network warning + mobile wallet block + wallet error/warning banners) was removed wholesale earlier today as part of the Buy-VPFI-into-app split. With no wallet UI on public pages, the ENS/Basenames-resolution gap doesn't exist anymore. AddressDisplay (which DOES resolve ENS) remains in use on every in-app surface where addresses appear, so the documented identity-resolution behavior is preserved where wallets are actually shown.

---
