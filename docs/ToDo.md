- [ ] Provide multiple multiple language

- [ ] For pyth, if we provide only price feed for ETH/USD, ETH/EUR, ETH/JPY, ETH/XAU and all other asset can be checked with respect to ETH right, so that it is not required for us to configure all the asset price feed right?
- [ ] we may also need to provide permission matching for bots to find two offers with matching conditions between lender offer and borrwer offer with same lending and borrowing asset type. and we may need to get max and min for duration, lending asset amount and borrowing asset amount from both lender and borrower during offer creation. together with with lender specifying min collateral amount (to have healthy HF and LTV) as he is the risk taker. Once these are all available, bots can match the offers based on conditions and the loan will be active. any way both users (lender and borrower) have provided consent during the offer creation itself. we should show how the HF/LTV will vary in worst case and good case condition based on max and min lending / collateral asset amount is fixed during offer creation, we may need to provide sliders and make things easy to understand. also system should decide on how much min collateral asset amount (minCollateralAmount) will be allowed based on lender max lending asset amount and provide him option to set minimum collateral amount required, only above the system set minCollateralAmount, also providing option max collateral amount. Like wise for borrower, system need to set max lending amount based on min collateral amount beyond which the borrower can't move the slider to ask for more, but borrower can still set max lending amount below the system set maxAmount, also borrower can set minum lending asset amount he needs. so the current values without range will look like only a maximum lending amount available and minimum collateral amount needed by lender and likewise for borrower, it is minimum lending amount needed and maximum collateral amount available. loan fields cannot have min and max values, only offers can have min and max values for lending asset amount, collateral asset amount, duration and interest. we can put these under advanced mode in front end. What do you say?
- [ ] update the release notes in docs/ReleaseNotes-2026-04-25.md and create new relase notes for today
- [ ] we need to provide slider to adjust the lending amount and collateral amount. also based on it, we should also show HF/LTV visually with animation
