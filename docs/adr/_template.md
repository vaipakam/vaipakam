# ADR-NNNN: <short imperative title — what was decided>

**Status:** Accepted | Superseded by ADR-XXXX | Deprecated
**Date:** YYYY-MM-DD

## Context

What was the situation forcing the decision? What constraint,
trade-off, or failure mode were we navigating? Cite the source files,
prior incidents, design docs (`docs/DesignsAndPlans/`), or external
references that frame the problem.

## Decision

State the decision in one or two paragraphs. Be specific — name the
component(s), the constant(s), the policy. A reader should be able
to identify the decision from this section alone, without having to
read the alternatives.

## Consequences

Both intended and accepted. Honest about the costs.

**Positive**

- ...
- ...

**Negative / accepted costs**

- ...
- ...

**Risks the decision creates**

- ...
- ...

## Alternatives considered

For each, summarise in 1-3 sentences what it would have been and why
it was rejected. The point is not to be exhaustive — it's to leave an
audit trail of "we did look at X; here's why we didn't pick it".

**Alternative A — <name>**: ... Rejected because ...

**Alternative B — <name>**: ... Rejected because ...

## References

- Source files: `path/to/file.sol:lines`, `path/to/file.ts:lines`
- Design doc: `docs/DesignsAndPlans/<doc>.md`
- Related ADRs: ADR-XXXX, ADR-YYYY
- Spec: `docs/FunctionalSpecs/<domain>.md`
- Issue / PR: #N
