{
  "name": "vaipakam-drift-subgraph",
  "version": "0.1.0",
  "description": "Minimal subgraph that indexes Vaipakam Diamond loan-state transitions to detect drift.",
  "license": "BUSL-1.1",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:base-sepolia": "graph deploy --node https://api.studio.thegraph.com/deploy/ vaipakam-base-sepolia",
    "deploy:local": "graph deploy --node http://localhost:8020 --ipfs http://localhost:5001 vaipakam-drift"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.84.0",
    "@graphprotocol/graph-ts": "0.35.1"
  }
}
