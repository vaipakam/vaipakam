# `engines.node` is ADVISORY to npm by default — it warns and installs anyway.
# This package's test command loads TypeScript via `--experimental-strip-types`,
# which needs Node 22.18+, so an under-spec environment would otherwise fail
# deep in the test run rather than at install time with a clear reason.
engine-strict=true
