/**
 * `@vaipakam/ui` — shared React components root entry.
 *
 * Per-component subpath imports (`@vaipakam/ui/InfoTip`,
 * `@vaipakam/ui/TokenIcon`) are also exposed via the package.json
 * `exports` map for consumers that prefer narrow imports.
 */
export { InfoTip } from './InfoTip.js';
export { TokenIcon } from './TokenIcon.js';
