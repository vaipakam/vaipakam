# Vaipakam alpha — frozen archive deploy on alpha.vaipakam.com.
#
# Source snapshot from main@f48c99fa5db2ff718344b929b62c5b7720590ae2,
# kept as a reference for the Stage 3 / Stage 4 refactor cutover
# window. The canonical site is https://vaipakam.com — every page
# served here has a duplicate at the canonical hostname (with the
# new architecture's bug fixes + features). To avoid splitting
# Google's ranking signal between the canonical and the archive,
# crawlers are told to skip alpha entirely.
#
# Stage A SEO scope explicitly excludes this surface: the sitemap
# under https://vaipakam.com/sitemap.xml lists only the canonical
# URLs.

User-agent: *
Disallow: /
