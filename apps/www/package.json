{
  "name": "@vaipakam/www",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Vaipakam marketing + docs Vite SPA — vaipakam.com (apex, canonical) with www.vaipakam.com 301-redirecting to apex via a Cloudflare Bulk Redirect rule. NO wallet connect, NO wagmi, NO on-chain reads (one read-only fetch of the indexer's published protocol-config snapshot for the documented fee/tier figures — no chain client, no ABI) — pure marketing surface populated by a literal copy of the marketing-side files from apps/defi (Landing, BuyVPFIMarketing, Whitepaper, Overview, UserGuide, Terms, Privacy, DataRights, Discord, HelpSearch). Public-read tools that live on the connected-app domain by industry convention (analytics, NFT verifier, protocol console) are linked out via `defiUrl()` rather than duplicated here. Hosted as a Cloudflare Workers Static Assets deploy on the same shape apps/defi uses, but with a tighter dependency surface (no connectkit, no wagmi, no react-query, no per-action permissioning). Folder, package name, Worker name were all switched together at the labs → www cutover (folder apps/labs → apps/www; package @vaipakam/labs → @vaipakam/www; Worker vaipakam-labs → vaipakam-www).",
  "scripts": {
    "dev": "vite",
    "prebuild": "tsx --tsconfig tsconfig.app.json scripts/check-knob-defaults-vs-contracts.ts && node scripts/generate-sitemap.mjs && tsx --tsconfig tsconfig.app.json scripts/generate-llms.mjs",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit && eslint . -c eslint.hooks.config.js && node scripts/check-guide-anchor-parity.mjs && tsx scripts/check-guide-toc-coverage.ts && tsx --tsconfig tsconfig.app.json scripts/check-live-value-render.tsx && node scripts/check-admin-knobs-mirror.mjs && tsx --tsconfig tsconfig.app.json scripts/check-knob-defaults-vs-contracts.ts && node scripts/check-content-links.mjs",
    "prerender": "node scripts/prerender.mjs",
    "deploy": "npm run build && npm run prerender && wrangler deploy",
    "sitemap": "node scripts/generate-sitemap.mjs",
    "check:anchors": "node scripts/check-guide-anchor-parity.mjs",
    "check:toc": "tsx scripts/check-guide-toc-coverage.ts",
    "check:admin-mirror": "node scripts/check-admin-knobs-mirror.mjs",
    "check:livevalue": "tsx --tsconfig tsconfig.app.json scripts/check-live-value-render.tsx",
    "check:knobs": "tsx --tsconfig tsconfig.app.json scripts/check-knob-defaults-vs-contracts.ts",
    "check:content-links": "node scripts/check-content-links.mjs"
  },
  "dependencies": {
    "@vaipakam/contracts": "workspace:*",
    "@vaipakam/i18n": "workspace:*",
    "@vaipakam/lib": "workspace:*",
    "@vaipakam/ui": "workspace:*",
    "i18next": "^26.0.8",
    "i18next-browser-languagedetector": "^8.2.1",
    "lucide-react": "^1.8.0",
    "mdast-util-from-markdown": "^2.0.3",
    "mdast-util-to-string": "^4.0.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "react-i18next": "^17.0.4",
    "react-markdown": "^10.1.0",
    "react-router-dom": "^7.14.0",
    "remark-gfm": "^4.0.1",
    "viem": "^2.48.4"
  },
  "devDependencies": {
    "@types/mdast": "^4.0.4",
    "@types/node": "^22.0.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vaipakam/eslint-config": "workspace:*",
    "@vitejs/plugin-react": "^5.0.0",
    "eslint": "^9.39.4",
    "playwright": "^1.61.0",
    "remark-parse": "^11.0.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.0",
    "unified": "^11.0.5",
    "unist-util-visit": "^5.0.0",
    "vite": "^8.0.11",
    "wrangler": "^4.0.0"
  }
}
