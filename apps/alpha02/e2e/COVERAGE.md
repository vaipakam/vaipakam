# alpha02 verification coverage matrix

One row per user-facing feature. Every behaviour-changing PR to
`apps/alpha02` updates this file in the same diff (like release-note
fragments and functional specs): add the feature's row, or extend an
existing one, stating WHERE it is verified.

**Tiers**

- **CI-Anvil** — automatic, every PR: a spec under `e2e/tests/` run by
  the `fork-tier scenarios` GitHub job against an Anvil fork of Base
  Sepolia. This is the DEFAULT tier; a feature lands here unless it
  genuinely cannot.
- **Live-only** — a committed driver under `e2e/live/`, run manually
  after the production deploy (the CLAUDE.md live-review DoD) and as a
  batch regression via `e2e/live/run-live-batch.mjs` before testnet
  releases. A live-only row MUST state its reason — typically a
  dependency Anvil cannot honestly fake (deployed Cloudflare Worker,
  Telegram, third-party API, real build-env wiring).

A feature may appear in both tiers (CI-Anvil for the flow mechanics,
live for the deployed-service half).

**What a live driver's exit code means** — 0 PASS, 1 FAIL (an assertion
against a page actually observed), 2 BLOCKED (could not observe, or
could not trust what was observed). The distinction is load-bearing for
every "Live review DONE" claim below: a driver that cannot reach the
chain must report BLOCKED, never PASS. So a driver must not convert an
RPC or transport failure into a domain answer — no "candidate raced
out", no "token burned" — because that hides a review it did not
perform behind an exit 0. Only an on-chain answer (a revert) may become
a domain verdict; anything else propagates to the BLOCKED path.

Two corollaries of "could not trust what was observed". A driver must
verify that the RPC it was pointed at actually serves the chain the
report names — nothing else checks it, since the injected wallet answers
the chain question locally, so a misdirected RPC otherwise reviews
another network consistently and exits 0 under the requested chain's
name. And every input to a pre-visit eligibility re-check must be read
at re-check time: a value cached during discovery can have changed under
it, and judging a page against a stale one turns a configuration change
into a reported product regression.

A live driver's output is pasted into the PR thread, so it is a
PUBLICATION surface: never print an RPC URL, a request URL or anything
else that can carry a provider key verbatim. Status lines get the origin
only; recorded URLs are redacted where they are recorded, not where they
are printed, so a later report line cannot reintroduce the leak.

Redact by HOST, not by the shape of the path. Deciding from the shape —
"segments over 24 characters look like keys" — is a denylist for
secrets: it has to anticipate every provider's key format, and the ones
it misses print in full (Blast and Chainstack key on a hyphenated UUID;
short hex keys fall under the length floor). The RPC's origin is known,
so any URL on it prints as its origin with the path discarded unread,
which covers a provider nobody has configured yet. Shape heuristics stay
as second-line defence for other hosts, where a legible path is worth
having. A redaction helper is only as good as its test: `redact.mjs` is
extracted from the driver precisely so `redact.test.mjs` can pin each
key shape in CI, rather than the guarantee resting on a throwaway script
someone ran once.

Classify transport-vs-answer by an ALLOWLIST of what counts as the chain
answering, never a denylist of known failures — the same argument the
read-only allowlist is built on. A missed operational code in a denylist
becomes a product FAIL blamed on the app; a missed one in an allowlist
becomes a BLOCKED, which is loud and harmless.

Read that evidence off the error CHAIN, not the top-level error object.
The RPC client wraps the provider's error, so the code and any revert
bytes generally sit on an inner cause; a predicate that tests the outer
object still works for the plain case and silently covers nothing else.
Because the miss costs only a false BLOCKED, this kind of dead clause
does not announce itself — it has to be measured against a responder
that produces each shape.

That allowlist is a THREE-way split, not two. Between "the chain
answered" and "nothing answered" sits a reachable provider REJECTING the
page's request as malformed — the JSON-RPC parse-error, invalid-request
and invalid-params codes. Those are positive evidence the endpoint is
working, and the fault is the page's, so they are a product FAIL. Filing
them with the transport failures exits BLOCKED and hides an app defect
behind a "re-run" verdict. Method-not-found is deliberately NOT in that
set: it describes what the server implements, not whether the request
was well formed. The classifier is extracted to `rpc-verdict.mjs` with
its own tests, for the reason `redact.mjs` was — a predicate verified by
a throwaway script grows silent bypasses.

Ask that same question of a RESOLVED response, not only of a thrown
error. The page reaches the chain by two doors — the injected provider,
whose failures throw, and the app's own HTTP transport, whose failures do
not. A rate-limited or rejected call comes back through the second door
as an ordinary response that `fetch` resolves happily, so a shim that
classifies only its `catch` block sees nothing wrong. Handed on
unjudged, that made a failed REQUIRED read look like a missing surface
(a product FAIL) and let a failed OPTIONAL read pass at exit 0 on a page
that was never fully served — the two outcomes the BLOCKED verdict
exists to prevent.

Two traps live in that check, and a naive "an error body means failure"
rule falls into both. The shim serves the WHOLE site, so only traffic
whose REQUEST is JSON-RPC may be judged by the RPC verdict — and the
request is also the only reliable discriminator, because a rate-limited
provider answers `429` with plain text that reveals nothing about what
was asked. And an ordinary REVERT arrives as an HTTP 200 carrying a
JSON-RPC error, so the same three-way split has to decide here too, with
'answered' recorded as nothing at all; anything less exits non-zero on
every healthy run. A batch is judged member by member and reported as
ONE entry per verdict, because the count is of page requests.

The unit of judgement is the CALL, not the HTTP envelope, and three
things go wrong when the envelope is treated as the unit. A non-2xx is
not automatically "no answer": a provider returning 400 with a
well-formed JSON-RPC error has received and rejected the page's request,
and the RPC client passes that straight through to the app — so it is a
client fault, and only a status the page itself cannot see past ends the
question. Mirror the client's own test for that exactly (both a numeric
code and a string message, on a single non-array body), because what
matters is what the PAGE experiences, not what seems reasonable. A batch
response that OMITS a member answers every call but one, and batches
resolve POSITIONALLY — a dropped member does not merely yield
`undefined`, it can hand one call another call's answer — so an
unanswered request id is unreachable. And two calls in one batch can
deserve different verdicts.

Positional resolution cuts the other way too: a SURPLUS or duplicate
member is worse than a missing one. Ask for ids 10 and 11, get back 9,
10 and 11, and the sort hands the first call the id-9 result while every
id asked for is present and looks answered — so an extra member poisons
the whole batch, where an omission costs exactly one call and leaves the
rest trustworthy. Nor is the absence of an error the same as success: a
member carrying neither a `result` nor a usable error object makes the
client hand the page `undefined`, which fails or renders degraded state
while contributing no verdict at all. Require a result, and require the
response ids to match the requested ids one for one.

Validate the request ENVELOPE before applying the method allowlist, and
share one definition of "is this JSON-RPC" between the gate and the
response classifier so the two cannot disagree. An empty batch satisfies
`every()` vacuously, so it rode through the gate and was then declined
by the response side — an invalid request the page itself made vanished
from the run. A member whose `method` is not a string failed the
allowlist lookup like any unsanctioned method, reporting a malformed
product request as a gap in OUR allowlist. Both are page defects and
must report as such.

A single attempt cannot settle any of it. The RPC client retries a
failed read by default and the app wraps its transports in a fallback
list, so the shim sees ATTEMPTS while the page experiences one logical
read. Judge per logical call — method plus params — and let a LATER
success clear an earlier failure. Only later: a read that worked and
then died for good is a genuine failure, and letting an early success
cancel it would hide exactly the mid-run degradation the drive exists to
notice. The driver's OWN reads need no such reconciliation, because
there the client exhausts its retries internally and throws once.

That reconciliation has a known limit, tracked as issue #1583: method
plus params is not invocation identity, so two independent reads that
share both — a repeated `eth_blockNumber` poll, say — are treated as one
logical call, and an unrelated later success can clear a failure that
genuinely reached a page. There is no sound fix at this layer: the RPC
client's retries are indistinguishable on the wire from independent
calls (each retry carries a fresh id), concurrent duplicate requests are
identical byte for byte, and no time threshold separates a retry chain
from the next poll. Closing it means taking operation identity from the
page rather than inferring it from the wire.

Checking OUR client's chain is not checking the PAGE's. The injected
provider answers `eth_chainId` locally, and the page's reads are neither
forwarded to the discovery RPC nor rewritten to it — the route handler
fetches each original request URL, which is whatever RPC the deployed
bundle was BUILT with. So a site built against the wrong network passes
the discovery check, and the surface it then fails to render is blamed
on the product; with deterministic deploys putting the same address on
both chains it can pass outright. Probe the endpoints the page actually
uses, and let only a definite mismatch block.

"The endpoints the page uses" is not "every endpoint the page touches",
though, and the first version of that check conflated them. The app talks
to two networks on purpose — an explicit chain-1 transport backs ENS
reverse lookups, one per counterparty on a connected page — so asserting
the review's chain against every endpoint declared a healthy site to be
built for the wrong network on essentially every connected run. Attribute
by positive evidence instead: an endpoint carrying a call ADDRESSED to
the Diamond is the one serving the deployment. Not by excluding known ENS
URLs, which come from the deployed bundle's own env and cannot be
enumerated from outside it — an exclusion list would be a guess that
silently stops matching.

A probe awaited at verdict time needs a deadline. The batch runner spawns
each driver with no timeout of its own, so an endpoint that serves the
page normally but stalls on a synthetic request would hold the whole live
review rather than produce a verdict. Expiry settles as "could not tell",
the same as any other unanswerable probe.

Every guarantee in that driver rides on HTTP interception, so RPC over a
WEBSOCKET bypasses all of it — the method allowlist, the response ledger
and the chain probe alike. The app prefers a WebSocket transport whenever
a deploy sets `VITE_*_WSS_URL` (none does today, and the live bundle was
checked to confirm it). Rather than pass a run that verified less than it
claims, the driver now watches for JSON-RPC frames on page sockets and
reports BLOCKED if any flowed. Gated on frames that are actually JSON-RPC
requests: a relay socket carrying other traffic says nothing about chain
reads, and a socket reset before its first frame has bypassed nothing,
because the fallback transport simply dropped to HTTP.

Two claims that did NOT survive checking, recorded so they are not
re-litigated. The production CSP is `script-src 'self'` with no
third-party origin, and the live HTML carries no Cloudflare RUM injection
at all — so there is no `POST /cdn-cgi/rum` beacon for the read-only gate
to trip over, and an exemption for one would be a hole guarding nothing.

Test the WIRING, not just the predicate. A correct verdict filed into
the wrong bucket is the same defect in a different coat, so the step
that turns the attempt ledger into the malformed-vs-unreachable buckets
is itself an exported, tested function rather than a few lines inline at
the call site.

The app-vs-infrastructure distinction only applies to PAGE traffic. A
driver's own discovery reads stay a two-way question: a malformed
request there is a defect in the driver, and "could not observe" is the
honest verdict for that too.

Browser and context SETUP is BLOCKED, not FAIL, and every step of it
belongs inside the same wrapper the discovery reads use — launching,
creating the context, installing routes, bindings and init scripts. Left
bare, the rejection reaches the top level and Node exits 1, which the
batch reports as a product defect from a driver that promised to tell
the two apart. No page was ever observed, so there is nothing to blame
on the app. A BLOCKED exit taken after the launch must also close the
browser.

A credential the drivers cannot USE is a BLOCKED precondition, not a
FAIL — and validating its shape is not the same as validating it. A
32-byte hex string is not necessarily a valid secp256k1 key (the
all-zero placeholder and anything at or above the curve order both pass
a regex and fail derivation), so the derivation itself has to be inside
the guarded path. Otherwise every signing driver exits 1 and the batch
reports an unusable wallet file as a possible product defect.

The three-verdict exit contract is honoured by EVERY driver in
`e2e/live/` as of #1581, and the batch runner classifies by an explicit
registry rather than by exit code alone. The registry stays even now that
it lists everything, because it is what makes the vocabulary honest: an
exit 2 from an unregistered driver reads as FAIL rather than being
promoted to BLOCKED, so the summary can never assert "ran but verified
nothing" about a driver that never agreed to mean that by exiting 2 — a
claim about a surface nobody checked. **Register a new driver when you
add one**; the runner names anything unregistered at startup instead of
quietly misreporting it later.

Which failures are preconditions, driver by driver:

- **The FIRST navigation is the reachability probe** — BLOCKED via the
  shared `visit()` helper. Later navigations stay FAIL: once the site is
  known to serve, a route that will not load IS a plausible regression.
  The line is "did we ever get served anything", not "did every
  navigation succeed". One deliberate exception: `live-ux-sweep.mjs` is
  an evidence sweep rather than a pass/fail drive, so it RECORDS a failed
  route navigation in its report and keeps going — its exit code speaks
  only to whether the sweep stayed read-only. Whether that should change
  is #1626.
- **Fixture data the drive reads rather than asserts** — the deployments
  bundle's addresses, the per-facet ABI bundle, an override artifact a
  flag points at. A stale or half-exported bundle means the surface went
  UNREVIEWED, not that it regressed.
- **On-chain state the drive needs in order to act at all** — most
  visibly wallet/vault funding. Gate it BEFORE the browser opens, on
  exactly the quantity the drive later asserts against, so the gate can
  never block a run that would otherwise pass.
- **Config that selects nothing** — a typo'd session or locale selector
  gathers no evidence, so the surfaces it names are still unswept.

Two failure modes to avoid when drawing that line. A precondition placed
AFTER the drive has mutated state exits past the cleanup boundary and can
leave a dev wallet dirty — every BLOCKED exit must sit ahead of the first
mutation. And the line must not be drawn so wide that it swallows real
defects: a chain that REVERTS a valid write is evidence, so only the read
half of a setup step belongs inside the precondition wrapper. The point of
separating the verdicts is to stop mislabelling in both directions.

Finding these is not a grep for `throw` and `process.exit`. The
misclassification that survived the first pass of #1581 went through a
driver's own step recorder — `record(step, 'FAIL', …)` — and reported "top
up the lender's vault" as a gasless-posting regression. Read what each
non-zero path actually MEANS, rather than trusting the shape of the exit.

Four more traps, each of which cost a review round on #1621:

- **Guard the WHOLE setup, not its first step.** Wrapping only the browser
  launch leaves the route shim, the wallet binding, the init script and
  the first page unguarded — a rejection there still exits 1 and leaks the
  Chromium. `launch()` routes every pre-page step through one wrapper.
- **A shared BLOCKED exit must not overrule a caller that has evidence.**
  A drive that launches repeatedly and accumulates findings across
  launches must be able to weigh a later setup failure against a real
  defect found earlier; exiting inside `launch()` silently deleted that
  rule. Hence `onSetupFailure: 'throw'` — default `'blocked'`, because
  forgetting the option must cost a blunt verdict, never a discarded
  finding. **Three drives are in this shape**, not one, and the rule has
  to be applied to each: `live-recover-locales.mjs` (a browser per
  locale), `live-ux-sweep.mjs` (a browser per session, accumulating
  read-only violations) and `live-desk-i18n-capture.mjs` (a context per
  locale). The test to apply: does this drive record anything before its
  LAST launch? If so, a mid-run setup failure must not decide the verdict
  alone. In all three the precedence is the same — a real finding outranks
  an incomplete run, and only a run that found nothing reports BLOCKED.
- **After a browser exists, use the ASYNC exit.** `blockedSync` skips the
  close, so a post-launch BLOCKED can strand a persistent-profile Chromium
  and leave its profile directory locked — which makes the NEXT driver in
  a sequential batch fail to launch, turning one blocked drive into
  several.
- **Presence is not validity, and "not empty" is not "complete".** A
  truthiness check on a deployment address accepts a malformed one and
  hands it to the RPC client, which throws exit 1 long before any
  address-shaped guard runs. A whole-bundle emptiness check on the ABI
  export passes a HALF-exported bundle. Validate the shape at load, and
  per file.

The write-permission version of the same point: `mkdirSync(recursive)` on
an existing directory succeeds without testing write access, so a check
that only creates the directory does not prove the artifacts can be
written. Probe an actual write. (Note this is invisible when the drive
runs as root, which ignores directory permissions — verify it as an
unprivileged user or the check looks like it passed.)

The transport rule covers EVERY way the page reaches the network, not
just the one the driver proxies. Reads the app makes through an injected
wallet do not travel the routed-fetch path, so a driver that records
unreachable traffic in only one place still reports the other as a
product FAIL. Wherever a page-initiated request can fail without an
answer from the node, that has to reach the same BLOCKED verdict.

| Feature | Tier | Where | Live-only reason |
| --- | --- | --- | --- |
| Wallet connect + network gate | CI-Anvil | `tests/01-connect.spec.ts` | — |
| Post lending offer (details → review → sign → on-chain) | CI-Anvil | `tests/02-post-offer.spec.ts` | — |
| Accept offer → loan initiation | CI-Anvil | `tests/03-accept-loan.spec.ts` | — |
| Repay loan | CI-Anvil | `tests/04-repay.spec.ts` | — |
| Cancel offer + cooldown | CI-Anvil | `tests/05-cancel-cooldown.spec.ts` | — |
| Faucet mint + watch-asset | CI-Anvil (e2e) + unit | `tests/06-faucet.spec.ts` — mint lands on-chain + watch-asset affordance; asserts the relabelled second-liquid row (#1103) labels its Mint button with the token's LIVE on-chain `symbol()`. The dynamic-vs-hard-coded label logic is proven deterministically by `src/lib/mintSymbol.test.ts` (#1111 — feeds a non-`mUSDC` symbol + the unresolved→generic fallback, which the fork e2e can't distinguish since its symbol IS `mUSDC`), run in the `defi vitest` gate | — |
| Write-path kill switch (#1056) | CI-Anvil | `tests/07-kill-switch.spec.ts` | — (build-env flag reproducible with a second dev server) |
| Pre-sign dry-run footer (#1058/#1059) | CI-Anvil + live | `tests/08-dryrun-footer.spec.ts` + `live/live-dryrun-review.mjs` | Live half re-checks the REAL RPC's revert-data shape — the #1059 bug was invisible on Anvil-adjacent assumptions and only surfaced live. |
| Friendly contract-error messages + tx-error diagnostics capture (borrow-error UX) | CI-Anvil (lib logic) + live | lib logic: `apps/defi/test/lib/decodeContractError.test.ts` (curated reachable-error copy, `humanizeErrorName` fallback, gas-trap only when no selector); alpha02 wiring verified live | The observable behaviour — a real revert (e.g. `MaxLendingAboveCeiling` from an under-collateralised borrow) rendering as plain-language copy in the dry-run footer + submit error instead of the raw selector, the reworded gas-trap message, and the failed tx landing in the diagnostics `lastError` sink / support report — needs a genuine on-chain revert the Anvil fork doesn't reproduce for this path. The decode/humanize logic itself is unit-tested in CI. |
| Translatable contract-revert messages (i18n) | CI-Anvil (unit) + live | `packages/lib/src/decodeContractError.i18n.test.ts` (English-by-default when no translator; stable key = error name/selector routed through the `translate` hook; catalog key ↔ decoder output parity; gas-estimate rewrite localizable) + `apps/alpha02/src/lib/errors.test.ts` (`submitErrorText` serves lib English by default, a `contractError.<key>` locale bundle overrides by stable key, an untranslated key falls back to English). Live half: with a non-English display language selected, a real revert's plain-language cause shows translated (or falls back to English until that locale's `contractError.*` bundle is filled). | A genuine on-chain revert through the borrow/lend flow isn't reproduced by the Anvil fork (same reason as the row above); the key-routing + fallback logic is unit-tested in CI. Per-locale translation of the messages is a backfill step (English-first). |
| Hardcoded position/vault/offer strings + locale-aware duration/date formatters (i18n) | CI-Anvil (unit) + live | unit: `src/i18n/*.test.ts` proves the reactive-copy proxy routes `tmpl` entries (incl. the new `copy.units.*`, `positions.details.terms*`, `vault.lockedFreeBreakdown`, etc.) through i18next with the English template as defaultValue and count-correct plurals; `i18n:template` + placeholder-parity keep en.json and every locale bundle in sync; `check-hardcoded-strings` (AST detector, #1365) guards against new inline strings. The #1393 burn-down extended this to the Basic surface — the offer-row line, step indicator, vault "on <chain>" suffix, accept-mode banner, security-leg labels, the "…? Switch" toggle, the VPFI "warming up" tier explainer, and the unknown-chain/symbol/network fallbacks are now catalog `tmpl` entries, translated across all 9 locales, leaving the detector's Basic-surface baseline empty (only the advanced Rate-Desk copy remained baselined). #1398 extended the detector to `.ts` helpers too, in a copy-call-arg-only mode (`src/i18n/hardcodedDetector.test.ts` pins the `.ts` mode: a `copy.*` arg is flagged, the JSX/object-key scans are skipped there), so a hardcoded fallback passed into a `copy.*` template from a plain helper can no longer ship untranslated. The Rate-Desk burn-down then emptied the desk baseline: the tape/history/positions loan-id + status-tooltip scaffolds, the mid-row "mid / spread" pieces, the match-band pair tooltip, the amend "Reading live values…"/"Close"/unit labels, and the order-ticket security-leg labels became `copy.desk.*` `tmpl`/label entries (incl. a `desk.loanStatus` map that localizes the raw indexer loan-status the tape tooltip shows — the one desk surface not already collapsed through `loanStateLabel`), translated across all 9 locales; only the AppShell release-stage badge (a proper noun) and non-copy dev diagnostics remain baselined. Live half: with a non-English language selected, My positions / My vault / loan-detail rows render the extracted strings translated, and durations ("7 days") + due dates show in the active language instead of English. | The observable payoff — previously-hardcoded strings and formatter output rendering in the chosen language — is only visible in the running app with a locale selected; the resolution mechanism and key parity are unit-tested in CI. Per-locale wording is filled for the 9 active locales (zh, ta, de, fr, es, ar, ja, ko, hi). The post-#1403 Rate-Desk wording pass (idiomatic + bundle-consistent mid/day/on-chain terms) is reviewed in `docs/DesignsAndPlans/RateDeskI18nLiveReview-2026-07-22.md`; its visual per-locale desk pass runs via the committed read-only driver `live/live-desk-i18n-capture.mjs` — **live-only** because it needs the deployed build + a real browser with the language actually switched (`<html lang>`/`dir` validated; exits non-zero on any locale that fails to load or switch), which the fork tier's stubbed HTTP harness can't reproduce. |
| Under-collateral precheck on the borrow terms step (#1112) | CI-Anvil (filter unit) + live (`e2e/live/live-collateral-precheck.mjs`) | filter logic: `src/lib/collateralPrecheck.test.ts` (only under-collateral error names warn; approval-needed / unrelated reverts / no-name / non-revert verdicts stay silent — no crying wolf); OfferFlow wiring (forced-consent `createOffer` eth_call on the terms step → `CollateralPrecheck` banner) verified live on alpha02.vaipakam.com — 100 liquidToken2 collateral against a 100,000 liquidToken borrow reverts `MaxLendingAboveCeiling` → banner shows; raising the collateral clears it | Same reason as the row above: a genuine under-collateral revert (`MaxLendingAboveCeiling` / `MinCollateralBelowFloor` / `InitLtvAboveTier`) through the multi-step borrow-post flow isn't reproduced by the Anvil fork, so the observable inline warning is verified live per the DoD; the decision logic is unit-tested in CI. NB the live driver needs the base-sepolia faucet mocks to classify Liquid (#1118/#1119) and a collateral $-value > 0 — a dust amount trips `maxLendingForCollateral`'s no-ceiling sentinel and (correctly) shows no banner. |
| Kill-switch zero-regression sweep | Live-only | `live/live-killswitch-regression.mjs` | Verifies the PRODUCTION build has no flows disabled — a property of the deployed Cloudflare build env, not of the code. |
| Telegram alert rails — LINK flow with wallet signature (#1055/#1056) | Live-only | `live/live-alerts-link.mjs` | Needs the deployed agent Worker (CORS origin gate, D1, signature verification) and the Telegram bot — no Anvil equivalent. |
| Telegram alert rails — unlink + signed due-date opt-out (#1056) | **Gap** | not yet driven; verified manually in PR #1056 (curl matrix in the thread) | Extend `live-alerts-link.mjs` with the unlink click and the opt-out toggle on next touch of the alerts surface. |
| GoPlus token security screen (#1049) | **Gap** | evidence: the 8-check scratch drive recorded in PR #1049; no committed driver yet (the full suite performs on-chain writes, unfit for the auto-run batch untrimmed) | Commit a batch-safe, read-only screening drive under `live/` on next touch of the GoPlus surface. |
| Token-risk badges on book + guided match, flagged-offer exclusion (#1036 badges slice) | CI-Anvil | `tests/13-token-risk-badges.spec.ts` — GoPlus doesn't index testnets, so the spec spawns a dev server with the test-only `VITE_GOPLUS_EXTRA_CHAINS=84532` knob and route-mocks the GoPlus origin: block row → "Risk flagged" book badge + guided-match exclusion with the honest hidden-count note; warn row → "Caution", still listed; 500 → "Not screened", still listed; control on the standard server pins the no-badges-on-testnets posture. The mocked-origin caveat is inherent (the real API has no flagged token on demand); the classifier itself runs unmocked. | — |
| VPFI vault deposit/withdraw dry run | **Gap** | not yet driven; the footer LOGIC is covered by `tests/08-dryrun-footer.spec.ts` on the offer path, but no test opens `/vpfi` | Add a `/vpfi` drive to 08 (or a 09) on next touch of the VPFI surface. |
| Support drawer: connection health + report-issue (#1028 item 4) | CI-Anvil | `tests/09-diagnostics.spec.ts` | — (health rows against the fork + stub; the crash→report path is exercised by seeding the ErrorBoundary's sessionStorage sink — a deliberate render crash has no production trigger, per the live-review DoD exception) |
| Offer Book on-chain catch-up merge (#1029) | CI-Anvil | `tests/10-offer-book-catchup.spec.ts` | — (the always-live stub can't lag, so the spec uses the stub's PIN mode to freeze the cache while the fork advances — honest manufactured ingest lag; also ABI-drift-guards every terminal-event selector) |
| Copy/legal: mandated disclaimer + consent inline links (#1030) | CI-Anvil | `tests/11-copy-legal.spec.ts` | — |
| Permit2 signature approvals (#1038) | CI-Anvil | `tests/12-permit2.spec.ts`, against the REAL canonical Permit2 on the fork: single-transaction property (Permit2 approval seeded + Diamond-allowance shortfall), silent classic path for wallets WITHOUT a Permit2 approval (hard-zero typed-data requests), and classic fallback on refusal (fixture reject flag). Specs 02/03 do NOT exercise the permit path — their wallets hold no token→Permit2 approval, so they run (and keep covering) the classic sequence. The accept/rent/VPFI-deposit permit wirings share the identical gate+two-phase pattern but only the post path has a spec (see the VPFI gap row). | — |
| CoinGecko market-listing soft signal on pasted addresses (#1036 fallback layer) | **Gap** | not honestly assertable on the fork: testnets have no CoinGecko platform, so the notice is structurally absent there BY DESIGN (`platformForChain(84532) === null`) — asserting its absence would only re-prove the gate, and asserting its presence would need yet another chain-override knob for a soft, non-gating line | Assert on a mainnet-platform chain when one is deployed; until then the pure `reputationNotice` mapping is the reviewed surface and the line is dormant on testnets by intent. |
| ENS reverse-name display (#1030) | **Gap** | not honestly assertable: fork wallets have no mainnet ENS names, so CI can only observe the hex fallback (which every address-rendering spec already exercises) | Assert with an ENS-named wallet if the dev wallet set ever gains one; until then the live review eyeballs the pill/rows for no regression. |
| Support ticket capture — widget contract (#1040 phase 1) | CI-Anvil | `tests/14-support-ticket.spec.ts` — second dev server with `VITE_AGENT_ORIGIN` pointed at a spec-local stub speaking the Worker's HTTP shape: consented send carries the REDACTED diagnostics block + returns a ticket number + ticket-carrying mailto; no consent → no diagnostics in the POST; 503 → plain-words failure + mailto fallback; control on the standard build shows the honest not-configured state. The Worker handler itself is pinned by apps/agent vitest (`test/supportTicket.test.ts`: validation, durable-row-before-notify, 503 on D1 failure, ops-bot skip). | — |
| Support ticket capture — deployed Worker + D1 + ops-Telegram (#1040 phase 1) | Live-only | `live/live-support-ticket.mjs` — sends one clearly-marked probe ticket against the real Worker; PASSES on either honest terminal state (ticket number + ticket-carrying mailto when provisioned, plain-words unavailable + mailto before migration 0028 / secrets land) and FAILS on any dishonest state (silent success, failure without the mail escape, disclosure missing page/network). Ops-bot delivery is eyeballed in the operator chat during the post-deploy review (the driver can't read Telegram). | Needs the deployed agent Worker (real D1 migration, real ops-bot secrets) — the stub tier above deliberately fakes exactly these. |
| ENS lookups — explicit mainnet endpoints, session-static, never the library default | CI-Anvil | `tests/15-rpc-diet.spec.ts` — whole-run assertion that zero requests reach `eth.merkle.io` (viem's implicit chain-1 default; a hit means the ENS transport regressed to `http(undefined)`). Endpoint fallback order and the once-per-session cache are not separately CI-assertable (no mainnet on the fork harness); the always-safe hex fallback is exercised implicitly on every page that renders addresses. | Not separately live-driven — the live RPC audit's budget would catch an ENS lookup storm, and the failure mode is cosmetic (hex fallback). |
| RPC diet — no streamed chain polling (parked book visitor) | CI-Anvil | `tests/15-rpc-diet.spec.ts` — counts JSON-RPC methods over a 15s steady-state window on `/offers` with no WS configured (the harness's and production's posture): `eth_blockNumber` ≤ 2 and `eth_getLogs` ≤ 2 (a watcher regression logs ~12+), and the book must still render. Idle backoff and the WS-mode push path are not CI-assertable (no interaction simulation over minutes; no WS endpoint on the fork harness). | `live/live-rpc-audit.mjs` — 60s steady-state tally against the deployed site with a hard call budget; run post-deploy and in the release batch. |
| Asset picker — faucet test tokens + SelectMenu dropdown | CI-Anvil | `tests/16-asset-picker.spec.ts` — on the fork (Base Sepolia's testnetMocks): all five faucet ERC-20s listed as rows with the "Faucet test token" badge, selecting one closes the menu and shows the live-resolved symbol on the control, and the paste-an-address escape hatch still opens and accepts input. Specs 02/03/08 keep exercising picker-driven flows end-to-end through the shared `chooseMenuValue` helper. Keyboard/AT semantics are code-reviewed (ARIA combobox pattern), not separately spec-asserted. | — |
| Rate Desk phase 1 (#1129) — /desk route: advanced-only nav (URL-reachable in Basic), market header (pair dropdown + custom-pair branch + tenor chips + quoted mid/spread), rate ladder (own-order highlight, top-of-book taker links, honest empty state), order ticket (GTC/Partial lend post), open orders (cancel + first amend UI, #193) | CI-Anvil + live | `tests/17-rate-desk.spec.ts` — book/ticket/amend/cancel per the #1129 DoD: nav gating both modes; a fresh (pair, tenor) market (tenor verified live-empty on the fork, so the inherited book can't pad the ladder) renders the honest empty state, then a direct-seeded two-sided book renders exact ask/bid rates + remaining sizes + own-highlight + the bid-side taker link, with the header quoting mid/spread; the ticket posts a GTC/Partial lend order (on-chain terms asserted) that lands on the ladder as own and under Open orders; amend reprices it via ONE `modifyOffer` (same offerId, live-seeded form) reflected in book + panel; cancel (past the 300 s cooldown via time travel) clears it from both while the seeded bid stays as the non-blank control. The stub gained `/offers/markets` + market-scoped `/offers/active` for this; the tape (`/loans/recent`) is deliberately still not stubbed (nothing asserts on it — it renders its honest unavailable state; the phase-2 routes cover the cross-status reads specs DO assert). The pure `buildLadder` math still has no unit test. Live half: `live/live-rate-desk.mjs` — the same journey against the deployed site + the real Base Sepolia Diamond with NO time travel: custom-pair market load at a PREFERRED live-empty tenor (`pickTenor` falls back to the least-populated bucket when every preferred WETH/tLIQ tenor already has live offers; the step record + run log state which mode the run used, and the book assert accepts either the honest empty copy or a populated ladder), post 0.002 WETH GTC/Partial at a distinctive rate, on-chain terms verified via viem, own-ladder + Open-orders rows, cooldown-gated Cancel (the driver attempts to capture the countdown copy — captured only when the post→verify stretch finishes inside the 300 s window; if the window has already elapsed by the UI check the step records OBSERVED instead), amend-in-place (same offer id on-chain), the REAL 300 s cooldown waited out, cancel + escrow refund verified on-chain; the indexer-backed surfaces are ASSERTED healthy — the markets summary must auto-select a real market (the degraded couldn't-load copy fails the drive) and the tape must render fills or the honest “No fills yet for this market.” empty copy (the unavailable copy fails the drive). | Live half exists because the fork can't prove the deployed build, the real RPC, or the real cancel-cooldown clock. First run (2026-07-10, post-#1134 deploy) passed with the indexer's market routes degraded (D1 migrations 0029–0031 not yet applied): the drive recorded the honest markets-summary + tape unavailable copy — the chain-path book was unaffected. Gap closed same day: after the operator applied migrations 0029–0031, the re-drive passed with real indexer data — the markets dropdown listed live markets and the tape rendered the true market-scoped empty state (“No fills yet for this market.”), with the full post→amend→cancel lifecycle green again. The gap is genuinely closed: per Codex #1138 round 2 the driver now ASSERTS both indexer-backed surfaces — a degraded markets summary or tape fails the run — with the 2026-07-10 post-migration runs as the passing baseline, so an indexer regression fails the next drive instead of being observed past. The driver's on-chain asserts were hardened per Codex #1138 round 1 (assets/amounts in the create check, escrow balance round-trip, end-paged offer index, duplicate-create detection, amend-preserves-terms full-struct compare, exactly-one-OfferModified event count). Round 4 made the cleanup sweep deterministic: before the final sweep the driver waits for the lender's pending nonce to settle to latest (the injected wallet is the account's only signer, so equal nonces prove no create is still in flight; instant when nothing is pending, CLEANUP-UNKNOWN red on ~120 s timeout), cross-checks the offer-index delta against an OfferCreated-log count for the creator, and — if one Post click ever produced more than one create — exits red even after successfully cancelling the duplicates, so a duplicate-post regression can't be masked by a clean sweep. |
| Rate Desk phase 2 (#1130) — executed-rate chart (lazy-loaded lightweight-charts: §5.3 honesty rules — candles only where fills exist, sparse-tape step-line + markers below 10 fills, fill-count+principal tooltip, dashed "quoted mid" overlay, last-fill header line instead of a %-change ticker; mobile Book\|Chart toggle) + History bottom tab (wallet's permanent desk activity via `/loans/by-participant`, all statuses, load-more pagination) | CI-Anvil + unit + live | `tests/18-rate-desk-chart.spec.ts`, on the stub's two new fork-hydrated routes (`/loans/rate-candles` via a cross-status loan walk mirroring the worker's fold; `/loans/by-participant` from the chain's own both-sides all-status loan index — participation = the loan's current parties, a documented fork-scale simplification since the fork has no position-NFT-transfer history). Sparse-tape honesty: a market verified BOTH offer-free and fill-free first renders the honest-empty chart (empty copy, NO series canvas, "no fills yet" header), then three direct-write fills a fork-day apart render sparse mode — the sparse note with the exact fill count, the last-fill header line (`last fill: 9.33% · …`, rate half pinned), the quoted-mid "resting quote" hint against a reseeded two-sided book, and the TradingView attribution link; the stub's fold is wire-shape-probed directly (three one-fill buckets, exact closes + BigInt-string principals). History: both seeded loans list under the History tab with the Lender role badge, "Due in …" status, and `/positions/<id>` deep links; after a direct-write full repay the row PERSISTS with the Repaid badge (the all-status gap the route exists to close) while the other loan stays live as the control. Canvas pixels aren't asserted — the §5.3 decision math stays unit-tested in `src/lib/rateChart.test.ts` (vitest gate), and the spec pins the DOM the honesty rules hang off. The layout assumption is asserted as a guard: the desk's breakpoints are CONTAINER queries (the shell sidebar eats ~350px), so the 1280px fixture viewport renders the ~810px mid-tier layout — chart full-width above book + ticket, mobile Book\|Chart toggle hidden — keeping every panel at full assertable width. | Live half SHIPPED in `live/live-rate-desk.mjs` (phases 1+2): step 2b asserts the chart card healthy (drawn series or either honest empty copy passes, the unavailable copy fails; interval/range chips + TradingView attribution asserted; `/loans/rate-candles` wire-probed with the UI's default 1d×30d params) and step 7b asserts the History tab healthy inside the cooldown window (rows or honest empty pass, unavailable fails; `/loans/by-participant` wire-probed with counts + roles, and UI emptiness must AGREE with wire emptiness). First production run 2026-07-10 post-migration-0032: all steps PASS — chart honest market-empty copy + clean wire probe on the fill-free driven market, History listing 7 backfilled all-status rows with Lender badges (wire agreeing), full offer #37 lifecycle green. Real NFT-transfer participants (which the fork can't produce) remain covered only by the worker's scratch-SQLite tests — extend the drive if a live position sale ever becomes part of a review. |
| Rate Desk phase 3 (#1131) — crossable-band previewMatch strip at the ladder mid (rendered ONLY on `previewMatch` errorCode 0 + `getMasterFlags().partialFill`, permissionless `matchOffers` execute), gasless signed-offer posting mode on the ticket (EIP-712 `Vaipakam SignedOffer` signature → `POST /signed-offers`, vault-free-balance warn-not-block), signed-book ladder merge ("Signed" badge, indexer-sourced rows), inline signed-order taker fill (`acceptSignedOffer` with order-hash-bound AcceptTerms, maker-funding preflight), own signed orders block in Open orders (market-scoped, on-chain `cancelSignedOffer`), live push-invalidation desk roots (KEY_MAP) + book delta row flash | CI-Anvil + unit + live | `tests/19-rate-desk-phase3.spec.ts`, on the stub's new `/signed-offers` pair (POST verifies the EIP-712 signature against the fork Diamond's exact domain like the worker's ingest gate + mirrors its 201/200-idempotent/400/409 answers; GET is market-scoped and LIVE-PROBES `signedOfferFilledAmount` + the burned nonce per row as the fork-scale substitute for the worker's event-driven lifecycle handlers — a filled order leaves the book on the next read). §5.2 both directions: a crossed-but-UNMATCHABLE book (rates cross, amount ranges can't overlap — previewMatch pinned to `AmountNoOverlap` off-chain first) renders the crossed mid note but NO band; a contract-confirmed Ok pair renders the band with the exact midpoint copy ("Matchable at 7.5%") and a THIRD wallet's Execute initiates the makers' loan on-chain (lender/borrower/principal/midpoint-rate/tenor/Active all asserted via `getLoanDetails`) and clears the ladder to the honest empty state. Gasless loop end-to-end: the maker posts from the ticket with ONE signature (`sentTransactions` pinned unchanged at the provider boundary — the gasless property itself), the stub's book row is wire-shape-pinned AND its orderHash cross-checked against the CONTRACT's `signedOfferOrderHash`, the Signed-chip row lands on the maker's ladder (own → no Fill affordance) + the own-signed block in Open orders (Cancel on-chain armed, not driven), then the borrower discovers the row, fills via the inline confirm (consent → allowance → `acceptSignedOffer`), and the spec asserts the loan carries the signed terms exactly, the on-chain fill ledger consumed the ceiling, the stub GET drops the row, the taker's ladder empties honestly, and History lists the new participation. Unit half (vitest gate): `src/lib/signedOffer.test.ts` (wire mapping, ceiling/remaining, ladder merge, candidate pickers — incl. the #1145 r2 single-value gate on the direct-fill affordance (ranged API-posted rows rest as matcher-only depth), `topOfBookMatchPair`), `src/lib/ladderFlash.test.ts` (delta-flash detection incl. market-switch/first-population silence), and `src/chain/pushKeyMap.test.ts` (the push KEY_MAP's desk-root registrations pinned exactly — offer frames → book/markets/amend-seed, loan.created → tape/candles/history/markets without double-invalidating the book, loan.updated → history, and the deliberate absences: deskSymbols, deskSignedBook, deskPreviewMatch). Push invalidation is NOT fork-assertable: the fork stub is plain HTTP with no WebSocket rail (the same no-WS posture spec 15 pins), so no invalidate frame can be delivered in CI — the mapping table is the CI-pinned half. | Live half — rails (i)+(ii) PASS 2026-07-11; rail (iii) VERIFIED for connectivity + frame delivery (local page-WS run, 2026-07-11), refetch attribution AMBIGUOUS-by-design with an optional full-PASS re-run documented below — no unobserved rail remains. Posting/cancel surfaces verified 2026-07-10 via the dedicated committed driver `live/live-signed-book.mjs` (live-rate-desk stays the phase-1/2 drive), on production alpha02 + the deployed indexer Worker (migration 0033 live) + the real Base Sepolia Diamond: the lender posted a 0.0001 WETH @ 9.31% GTC gasless order — Partial→AON auto-flip observed on the ticket, ONE `eth_signTypedData_v4` and ZERO `eth_sendTransaction` (pending nonce pinned), success copy rendered (deployed nuance: the copy does not print the order hash — the driver captures it deterministically from the page's `POST /signed-offers` response and keys every lookup/cancel/ledger read on it, with pre-existing own rows snapshotted at preflight and reported, never auto-cancelled); the row landed on `GET /signed-offers` (200 + `no-store`) with the single-value-AON collapsed wire shape and its `orderHash` reproduced exactly by the Diamond's `signedOfferOrderHash` view; the ladder showed the level with the Signed chip + own marker and the Open orders own-signed block listed it with Cancel on-chain armed; `cancelSignedOffer` confirmed NO cooldown and poisoned `signedOfferFilledAmount` to the ceiling. The crossable-band strip was honestly ABSENT on the un-crossed live book (its matchable loop stays fork-only). The first day's post-cancel observations were blocked by the #1149 production ingest stall (the OfferModified phantom-column wedge; the driver's step-1b freshness probe + step-7 stall-vs-regression timeout branch diagnosed it live). After the #1150 fix deployed, the cursor caught up ~25.7k blocks with ZERO event loss — the stalled drive's cancel was scanned exactly once ~18 h late and its GTC row left the book (observation (i) proven on the incident order itself). The 2026-07-11 re-drives then closed both scan-fed rails from the committed driver: (i) PASS — the fresh cancel's row left `GET /signed-offers` +130 s after cancel submission; (ii) PASS — `offer.changed` rode the CROSSING `invalidate` frame (+128 s, `scannedTo` 43980744 ≥ cancel block 43980719) with Node-socket continuity over the whole cancel-submission→frame window and a CLEAN same-scan confound sweep over blocks 43980571..43980744. Two driver hardenings shipped with that close: the confound sweep now anchors its window floor on the step-1b cursor probe when the crossing frame is the first frame observed (a conservative SUPERSET window — previously that first-frame case was structurally AMBIGUOUS even on a provably clean quiet book), and the step-7 scan wait spans TWO ingest cycles (600 s) so a cancel landing just after the chain's round-robin tick — or residual post-stall catch-up — can't burn a full post+cancel gas cycle on a DEFERRED. Rail (iii) — the BROWSER push path (page WS → `IndexerPushSync` → book refresh before the poll) — CONNECTIVITY + FRAME DELIVERY VERIFIED, refetch attribution AMBIGUOUS-by-design. The review sandbox could never make this observation (its proxy blocks the page WebSocket; the 2026-07-11 sandbox runs recorded the page socket never reaching OPEN), so it was closed by a **local page-WS-capable run** of the same committed driver (2026-07-11, operator-driven; report + remote-session on-chain verification on issue #1149): the page WS reached OPEN, delivered the crossing `invalidate` frame carrying `offer.changed` at the SAME `scannedTo` (43985676) the Node-side observer recorded, and a `/signed-offers` refetch started 0.3 s after the frame — the deployed `IndexerPushSync` rail demonstrably connects, receives, and maps frames in a real browser. The driver withheld the full PASS by its own anti-false-positive rule: the previous book fetch was 25.9 s earlier, so a 30 s `useDeskSignedBook` poll tick was plausibly due inside the message→refetch window — push vs poll cannot be attributed from one sample (worth noting, not crediting: a poll anchored at the prior fetch would fire at +30 s, and the refetch started 4 s BEFORE that). Every on-chain claim of that run was independently re-verified by the remote session (cancel receipt, ledger poisoned to ceiling, single-log confound sweep, row absence). REMAINING (optional, cosmetic to the rail's health): a re-run where the crossing frame lands clear of a due poll tick (~5-in-6 odds per run on the 30 s interval) upgrades (iii) to a full PASS; the driver needs no changes for it. |

| UX trust batch 1 (Findings20260711 UX-001/002/007/020/021/022) — state-aware loan receipts (terminal loans stop showing a live "Owed" or default warning), exact claim amounts from `getClaimable`, Support FAB bottom-left on phones, header address chip nowrap, Try-again on unavailable states, spinning loaders | Live (gap row) | Not separately CI-spec-asserted: copy/state-level changes with no new flow; the existing position/claim specs keep exercising the surfaces. Follow-up: assert the repaid-loan "What happens next" copy + claim-amount title in the positions/claims specs on their next touch. | Post-deploy `live-ux-sweep.mjs` re-run: `/positions/16` (repaid) must show "What happens next" with no default warning; Claim Center titles must carry amounts; mobile Claims screenshot must show the FAB clear of the Claim buttons; header chip on one line. |

| UX mobile batch 2 (Findings20260711 UX-006/019/039/042) — desk single-column stack below 560px container width, item-row cards stack with full-width CTAs ≤480px, compact wizard-step line on phones, CopyAddress chips (copy + explorer, 44px touch targets) on Vault + Faucet | Live (gap row) | CSS/markup-level changes; existing desk + flow specs keep exercising the surfaces at the 1280px fixture viewport (which renders the mid-tier layout, not the stacked one — container queries make the phone tier unreachable at fixture width). Follow-up: a 390px-viewport desk spec when the fork tier next grows a mobile fixture. | Post-deploy `live-ux-sweep.mjs` re-run: `basic-mobile--desk.png` must show ladder ABOVE ticket full-width (no side-by-side crush); `basic-mobile--offers.png` cards must stack text-then-button; a flow page mobile capture must show "Step N of M" instead of wrapped dots; Vault/Faucet captures must show the copy chips. |

| UX risk-visibility batch 3 (Findings20260711 UX-003/004/030) — position-list badge escalates on poor health (worse-only override of the time badge), past-due loan detail shows a live grace-countdown danger banner (switching to grace-expired wording), "If nothing happens" + consent copy gloss grace/liquidation/HF/LTV jargon inline with the concrete window length | Live (gap row) | Not separately CI-spec-asserted: the fork tier's loans are freshly initiated (healthy HF, never past due), so neither the health override nor the grace banner is reachable without HF/time manipulation fixtures the suite doesn't have yet; the copy-level glosses ride surfaces the existing specs already render. Follow-up: when the fork tier grows a time-travel/oracle-drop fixture (same need as the liquidation-path specs), assert the badge override at a forced low HF and the banner across the grace boundary via `evm_increaseTime`. | Post-deploy `live-ux-sweep.mjs` re-run + manual check of a live past-due loan if one exists: the loan detail "If nothing happens" row must state the concrete grace length; an overdue loan must show the countdown banner; Advanced-mode Health row must show the glossed HF/LTV parenthetical. |

| UX dead-ends/discovery batch 4 (Findings20260711 UX-010/011/023/024/026/032) — faucet link on failing balance checks, persistent mode switch (sidebar footer + More sheet), real phone More sheet replacing the Settings-alias tab, forward CTAs on Vault/Claims/Rent/faucet-success empty states, Positions grouped Needs-attention/Active/Ended with chain-confirmed "Claim waiting" chips, Basic-mode PowerSurfaceNote on /offers + /desk, NFT verifier in the nav, viewer branch for the "If nothing happens" row | CI-Anvil (existing specs) + live (gap row) | Nav/empty-state/copy-level changes ride surfaces the existing specs already render at desktop width (where the sheet and tab bar are hidden); the Positions attention-grouping depends on a claimable loan, which specs 03/04's repay flow produces — assert the "Claim waiting" chip + grouping in those specs on their next touch. The More sheet needs the 390px-viewport fixture already named by the batch-2 row. | Post-deploy `live-ux-sweep.mjs` re-run: /positions must show the grouped sections (lender wallet has claimables), /offers + /desk in Basic must show the orientation note, mobile passes must show the More button; manual: More sheet opens/lists/closes, mode switch flips without navigating, faucet success shows next-step links. |

| UX performance batch 5 (Findings20260711 UX-005) — static HTML boot splash, React.lazy route chunks under a Suspense'd shell, wallet/React vendor-chunk splitting, `vite:preloadError` stale-chunk reload | Live (gap row) | Build-config + HTML change with no new behaviour. Specs that DO navigate the now-lazy routes keep covering them: `/positions` + `/positions/:id` (03/04), `/offers` (10), `/desk` (17/18/19), `/claims` (via 04's repay→claim), `/faucet` (06). NOT directly navigated by any spec — `/rent`, `/vault`, `/activity`, `/vpfi`, `/settings`, `/nft` — so a lazy-chunk regression on THOSE is a live/manual gap, not CI-covered (Codex #1169 r1). Follow-up: add a smoke spec that visits each currently-unnavigated lazy route and asserts it mounts (catches a broken dynamic import). The boot splash appears only before the bundle executes, which the fork server can't reproduce (chunks serve instantly on localhost); the `vite:preloadError` reload needs a post-deploy hash change no single fork run produces. Follow-up: a throttled-network spec asserting `#boot-splash` paints before `#root` has React content, if the harness gains network-condition control. | Post-deploy: measure the served entry-chunk size (must be the split ~118 KB bundle, not the monolith) and confirm `#boot-splash` markup ships in `index.html`; a throttled DevTools load should show the splash then the app; smoke-visit the six unnavigated lazy routes. |

| UX Activity rebuild batch 6 (Findings20260711 UX-008/050) — event label map + acronym-safe humanizer, per-transaction coalescing, substance sub-line + explorer tx link + relative time, load-more pagination; Positions→Activity link + indexer-degraded chain-authoritative fallback | CI-Anvil (unit) + live (gap row) | The pure feed logic is unit-tested in `src/lib/activityView.test.ts` (vitest gate): the humanizer's acronym handling (the `NFTMinted`→"NFT Minted" bug), the cancelled/canceled normalization, and `coalesceByTx` (highest-priority representative, tie-break by logIndex, no-txHash rows never merged, newest-first). The PAGE-of-25 reveal and the explorer-link markup are DOM wiring over that logic. Not fork-e2e-asserted: the fork tier's activity endpoint isn't seeded with a rich multi-event history, so the coalescing/label rendering has no realistic feed to render against; the Positions→Activity link + degraded-fallback copy ride surfaces the existing specs render. Follow-up: seed a multi-event fixture (loan-init tx with its `*Details`+`Transfer` companions) in the stub and assert one coalesced row + the "+N more" note on Activity's next spec touch. | Post-deploy `live-ux-sweep.mjs` /activity pass: rows must show readable labels (no raw `Nftminted`), relative times, explorer links, and the load-more button when >25 rows; /positions must show the "See your full activity history" link. |

| RPC read-diet PR 0 (Alpha02RpcReadDietDesign §9) — `ownership.changed` push key mapped onto the holder-keyed roots (myLoans/myOffers/claimables/positionOwners/loan/offer) and `loan.updated` extended with `vaultAssets` | CI-Anvil (unit) + live (observation gate) | The frame→root table is pinned exactly in `src/chain/pushKeyMap.test.ts` (vitest gate), same posture as the #1131 desk-root rows: the fork harness runs the indexer STUB with no WebSocket rail (spec 15 pins that), so a push frame can never be delivered in CI. The indexer half (counting ownership re-points + entitlement-only mutations, the cursor heartbeat, hello/stats cadence metadata) is unit-tested in `apps/indexer/test/invalidationKeys.test.ts`. | Live half: per the design's §7(c) gate, transfer a position NFT between the two dev wallets on the deployed site and observe the `ownership.changed` frame arrive + dirty My positions/Claims on the production WS rail — REQUIRED before PR A removes the per-block blanket that currently masks this key's absence. |

| RPC read-diet PR A (Alpha02RpcReadDietDesign §4 phase 1) — rail-health-gated intervals (signalAware/tipAware), LiveChainSync tip-subset demotion, bookGhostStrip split, anchored desk cooldown clock (fail-closed confirm), centralized cross-tab receipt floor, row-action blocking preflight, Claims content-hash re-key (the VITE_FRESHNESS_TIMERS=legacy rollout hatch shipped with this row and was REMOVED post-§7-live-review — rail-down fallback is the permanent legacy posture) | CI-Anvil (unit + fork fallback posture) + live (DoD) | `src/chain/railHealth.test.ts` pins the health truth table (unknown cadence ⇒ never healthy; wedged persisted stamp ⇒ decays; reconnect re-proves; tipAware needs BOTH rails). The fork tier has no WS rail (spec 15 pins that posture), so specs inherently assert the FALLBACK side: 30s intervals, flows refetching on own receipt, Claims verifying on chain — `tests/15-rpc-diet.spec.ts`'s steady-state budget still applies unchanged. | Live (design §7 DoD, post-deploy): (a) 10-min idle RPC count before/after, (b) detail-page action gates at ~12s tip parity vs list rows at push latency, plus the row-action preflight blocking a just-consumed cancel, (c) ownership.changed frame observed with the tip nudge out of the picture, (d) WS blocked ⇒ 30s + degraded note return, (e) ingest stalled with live socket ⇒ demotes to 30s (no false-healthy 180s). |

| RPC read-diet PR B (design §4.2.1) — display protocol-config (fees bundle idx 0/1/14, rental buffer idx 6, master flags) served snapshot-first from GET /config/:chainId with live-chain fallback; submit paths unchanged (readLiveProtocolFees / readRentalBufferBps stay chain-only) | CI (unit) + live | `apps/indexer/test/configSnapshot.test.ts` pins the refresh decision (explicit governance-event allowlist incl. lifecycle-name negatives, bootstrap, 6h backstop, quiet-scan skip, near-head guard, monotonic upsert/stale-mark). The alpha02 hooks' snapshot-first-with-fallback branch is exercised implicitly on the fork tier: the stub serves no /config route, so fetchProtocolConfig returns null and every existing spec runs the chain-fallback path — the pre-PR posture. | Live (post-deploy): GET /config/84532 returns the bundle; /help fee card and rental-buffer copy match the deployed values; flip nothing — just confirm the display values equal a manual getProtocolConfigBundle read. |

| VPFI tier table snapshot-first (#1238, PR B follow-up) — useVpfiTierTable reads bundle idx 7/8 from GET /config/:chainId with live-chain fallback; display-only (fee settlement is on-chain) | CI (unit) + live | `src/data/parseTierSlots.test.ts` pins the snapshot slot parser (decimal-string uint256[4] round-trip; null on arity/shape/non-numeric surprises → chain fallback). Fork tier serves no /config route, so every existing spec runs the chain-fallback path — the pre-#1238 posture. | Live (post-deploy): /vpfi tier table renders the deployed thresholds/discounts with no getProtocolConfigBundle eth_call attributable to it in the page's multicalls (useVpfiTierTable mounts only on /vpfi — the Help page has no tier table). |

| RPC read-diet PR C (design §4.2.3) — Claims fan-out reduction: per-candidate memoized verdicts (identity-keyed, cleared on ownership.changed frames + receipt invalidations, session-scoped, transport failures never cached) + the ADDITIVE GET /claim-candidates/:address hint (capped at 200, truncation-honest) consulted only as fallback discovery when the chain enumeration is unavailable (never suppresses chain enumeration) | CI (unit) + live | `apps/indexer/test/claimCandidates.test.ts` pins the route (per-held-side flat rows, recency ordering, terminal-only incl. internal_matched, burned-side exclusion, 200-row truncation-honest cap, bad-address/unconfigured-chain rejects); `src/data/claimVerdictCache.test.ts` pins the memo contract (miss-first/hit-identical incl. null verdicts, bump forgets all, cap resets instead of growing). The fork tier's stub serves no /claim-candidates route, so every existing Claims spec runs the no-hint path — and the memo is exercised inherently by any spec that revisits Claims (verdict reuse is behaviour-invisible by contract). | Live (post-deploy): on the Claims page, a quiet re-check spends ONLY the discovery reads (the getUserPositionLoansPaginated page(s), plus live-status probes for any reversible-status rows) with ZERO per-candidate ownerOf/getClaimable/rebate probes (DevTools filter on the RPC host) — discovery is not memoized, so do not expect literally zero reads; transfer a position NFT between dev wallets and confirm the recipient's Claims row appears after the ownership.changed frame (memo cleared, re-probe ran) without a reload. |

| RPC read-diet PR D (design §4.2.2) — scoped push hints: frames carry bounded affected loan/offer ids + causative links (consumed offerId, parties); the client drops OWN-position roots (myLoans/myOffers/claimables/vaultAssets) from a frame's invalidations only when the hint is complete (truncated === false) AND provably irrelevant to the wallet | CI (unit) + live | `apps/indexer/test/pushHints.test.ts` pins the collector (id extraction incl. the internal-match triple, causative links, truncated forced by Transfer/signed-lifecycle/unrecognised events and by the cap); `src/chain/pushHintScope.test.ts` pins the client rule (narrow only on complete+irrelevant; every ambiguous input — absent/truncated/malformed hints, unknown wallet or cache — keeps the coarse set). Fork tier has no WS rail, so specs keep exercising the frameless posture. | Live (post-deploy): with two wallets in separate browsers, a foreign accept/cancel does NOT refetch the idle wallet's myLoans/claimables (DevTools indexer-request filter), while the counterparty wallet's tabs still update at push latency; a position-NFT transfer (truncated frame) refreshes BOTH — the honest-degradation case. |

**Gap rows** are allowed but must name the follow-up ('extend X on next touch of Y') — an honest gap beats a false claim; the matrix only works if it never lies.

| UX desk order-ticket batch 7a (Findings20260711 UX-009/016/027) — first-blocking-reason line under Post + Connect button when disconnected, "terms changed — re-confirm" note beside the auto-cleared consent box, Max chip on the escrowed leg, fees & commitment summary before consent | Live (gap row) | Copy/logic/CSS on the existing ticket surface; spec 17 (`tests/17-rate-desk.spec.ts`) keeps exercising the ticket's post/amend/cancel mechanics. Follow-up: extend spec 17 to assert the block-reason line on an incomplete ticket, the Connect button when the fixture wallet is disconnected, and the Max-fill + fee-summary rows, on its next touch. | Post-deploy `live/live-rate-desk.mjs` (or an eyeball pass): a disconnected/empty ticket shows the reason line (and a Connect button when no wallet), editing a term after ticking consent shows the re-confirm note, the Max chip fills the escrowed leg from balance, and the Fees & commitment summary renders the committed leg + the side's protocol fee. |

| UX desk polish batch 7b (Findings20260711 UX-028/036/037/038/045/046) — ladder rate as a real button (un-nested Take/Fill), right-aligned tabular fixed-2dp rates, neutral ask/bid palette, own-order screen-reader label, "Depth" header, live-dot tenor chips, chart credit only when a chart draws, match-band gas note, honest open-order fill % + remaining | CI-Anvil (partial) + Live | `tests/17-rate-desk.spec.ts` already asserts the ladder rate cells (`.desk-rate-ask`/`.desk-rate-bid`, own-highlight, taker link) and was updated in this diff to the fixed 2-decimal rate form ("8.00%", not "8%") the ladder now renders; the un-nesting keeps those locators valid (rate is a button inside the row). `tests/18-rate-desk-chart.spec.ts` now asserts the TradingView credit is ABSENT on the honest-empty chart and PRESENT (with href) once a series draws (UX-037), and `tests/19-rate-desk-phase3.spec.ts` asserts the match band's gas-note body (UX-045). Follow-up: extend spec 17 to assert the "N left" remaining-size text on a partially-filled order and the `<button class="desk-rate-btn">` role on its next touch. | Post-deploy `live/live-rate-desk.mjs` (rates already 2-dp: `POST.pct` `8.43%` / `AMEND.pct` `11.27%`) + eyeball: neutral ladder palette, "Depth" header, live-dot chips, credit gated to a drawn chart, match-band gas note, and a partially-filled order's rounded % + remaining. |

| UX a11y + chrome batch 8a (Findings20260711 UX-013/031/044) — "Skip to content" link + focus-to-main on route change + 404 top-level heading, persistent network chip when connected, readable Help build date | Live (gap row) | App-shell chrome + a11y with no new flow; existing specs keep exercising navigation, and spec 15 already asserts the `/offers` `h1`. Follow-up on next shell-spec touch: assert the skip link is the first focusable element and targets `#main-content`, the 404 renders an `h1` (`getByRole('heading', { level: 1 })`), the network chip shows the chain name when connected, and the Help footer shows a formatted date (no raw ISO `T…Z`). | Post-deploy eyeball / `live-ux-sweep.mjs`: Tab from the top reveals "Skip to content"; navigating pages moves focus to the content; the header shows the network name when connected; the Help footer build date is human-readable. |

| UX guided-flow + offer clarity batch 8b (Findings20260711 UX-014/018/025/040/041) — early "connect wallet" prompt on the flow's first step, role-specific offer CTAs ("Borrow this" / "Fund this request"), role-checking action placeholder, shared EmptyState for empty matches, "Post another" on the post-flow Done screen | CI-Anvil (partial) + Live | The existing accept/post specs keep exercising the flows; they select offers by `?offer=`/`"offer #id"` + "Choose" (never the card CTA text) and connect the wallet before the details step, so the CTA rename and the disconnected-only early prompt don't affect them. Follow-up on next flow-spec touch: assert the role CTA text ("Borrow this" on a lender offer, "Fund this request" on a borrow request), the early connect prompt when the fixture wallet is disconnected, and "Post another" resetting the post flow. | Post-deploy eyeball / `live-ux-sweep.mjs`: a disconnected borrow/lend flow shows the connect prompt on step 1; an offer card reads "Borrow this"/"Fund this request" by side; a loan detail shows "Confirming your role…" briefly on load; the empty matcher shows the icon+heading empty state; the post Done screen offers "Post another". |
| UX VPFI/faucet/nav polish batch 8c+8d (Findings20260711 UX-017/029/034/035/047/048/049) — VPFI deposit toggle + wallet-balance row, exclusive tier-table bounds + sub-100 note, faucet card flatten, page-title↔nav alignment, malformed-address input hint + dimmed disabled primary, Rent browse CTA, +5 Help FAQ entries | Live (gap row) | Copy/CSS/markup on surfaces the existing specs already render; no new flow. The nav/title strings are captured only in the live `report.json`, never asserted by a spec (verified via an e2e-safety grep in this diff), so the renames don't break specs 15/17/etc. Follow-up on next touch of the vpfi/faucet/help specs: assert the half-open tier bounds ("1,000 – <5,000 VPFI"), the flattened faucet single-card, the aligned titles, and the malformed-address hint. | Post-deploy `live-ux-sweep.mjs` / eyeball: VPFI shows the toggle switch + "in your wallet" row + one-row-per-threshold table with the sub-100 note; Faucet is one card of rows; page titles read "Claims"/"My vault"/"VPFI discounts"/"NFT verifier"; a bad pasted token address shows the plain-words hint; Rent shows the browse CTA; Help lists the five new FAQ entries. |
| UX2 batch A (Findings20260713 UX2-001/002/006) — connected phone header can no longer widen the page (shrinkable chip + phone-tier trims), splash failure state (HTML-resident timer → message + Reload), one-line Connect label | CI-Anvil (UX2-001/006) + live (UX2-002) | `tests/20-mobile-header.spec.ts` — 390px whole-document no-sideways-scroll invariant, connected AND disconnected (the wallet fixture gained `preAuthorized:false`, without which the injected wallet reports accounts pre-approval and the disconnected state is unreachable in this tier), plus the one-line Connect-label assertion. | UX2-002's observable behaviour (splash stuck on a dropped chunk → message + Reload after 20s) needs a real failed-asset boot no fork run reproduces on demand; the handler's presence in the built HTML is verified at build time, and the full path is a post-deploy DevTools check (block a chunk request, watch the fallback). |
| UX2 batch B (Findings20260713 UX2-003/004/005/007) — Settings "More" cards match nav ("Claims"/"My vault"/"VPFI discounts"), VPFI discount consent is a real switch (track + sliding thumb), faucet/VPFI dead-ends offer a one-click "Switch to <chain>" for connected wallets, Activity true-empty gets Borrow/Lend CTAs | Live (gap row) | Copy/CSS/markup + chain-switch wiring on surfaces the existing specs already render; no new flow, and the switch-chain CTA needs a connected wallet on a NON-target chain (Arb Sepolia → Base Sepolia) which the fork tier doesn't stand up. The chain-target resolvers are populated-field driven (faucet: `testnetMocks`, VPFI: `vpfiTokenImpl` — the canonical-only signal, since `isCanonicalVPFI` isn't exported; both asserted populated in this diff). Follow-up on next touch of the vpfi/faucet/settings specs: assert the renamed Settings cards, the switch-role of the consent control, and the "Switch to <chain>" button presence when connected off-target. | Post-deploy `live-ux-sweep.mjs` / eyeball: Settings "More" reads "Claims"/"My vault"/"VPFI discounts"; the VPFI consent renders as a switch (not a checkbox); on Arb Sepolia the faucet-unavailable and VPFI-not-registered states show a working "Switch to Base Sepolia" button; a brand-new wallet's Activity shows clean empty copy + Borrow/Lend CTAs. |
| UX2 batch C — perf tail (Findings20260713 UX2-007-tail/UX2-008) — the ~761 kB Diamond-ABI split into its own `contract-abis` chunk and lifted off first paint by gating each ABI consumer to when the ABI is actually wanted (Borrow/Lend lazy routes; Home's positions banner + shell SanctionsBanner mount only when connected, each behind a quiet ErrorBoundary; Help's live-fee card mounts only when connected, disconnected shows non-committal fee copy); Activity's hedged empty title softened and kept for every truncated case | Build-artifact + Live | UX2-008 is a bundler-graph + runtime property: verified at RUNTIME (headless Chromium against the built dist) that a disconnected `/` and `/help` fetch NO `contract-abis` request while `/borrow` (an action route) does, and that `/help` shows the non-committal fee copy; the entry `modulepreload` set is ABI-free. UX2-007-tail is a copy change with no wallet-state branch to assert beyond the existing empty-feed coverage — the hedge now shows for every truncated empty feed with recent-only wording, so there is no "clean title for zero-loan wallets" behaviour to test. Follow-up on next touch of the activity spec: assert the recent-only hedge wording (no "older events may exist") on a truncated empty feed. | Post-deploy `live-ux-sweep.mjs` / DevTools: on a cold disconnected Home/Help load the Network panel shows the entry + vendor chunks but NOT `contract-abis` until a wallet connects or an ABI-reading route is opened; `/help` while disconnected shows the fee structure with "connect for exact current rates"; a truncated empty Activity feed shows the recent-only wording plus Borrow/Lend CTAs. |
| Second-pass UX sweep sessions (Findings20260713) — `live-ux-sweep.mjs` now runs sessions × passes: the three connected-Base passes PLUS a genuinely-disconnected first-visit session (driver `preAuthorized:false` — `eth_accounts` returns `[]` until `eth_requestAccounts`, like a real un-approved wallet) desktop+mobile, and a connected Arbitrum Sepolia session over the chain-scoped routes; `UX_SWEEP_SESSIONS` selects sessions | Live-only (review tooling) | `live/live-ux-sweep.mjs` + `live/driver.mjs` — evidence generator for review nights, not a pass/fail spec; the 2026-07-13 run caught UX2-001 (connected-mobile header overflow) and UX2-002 (splash has no failure state) precisely because of the new sessions | The sweep's subject is the DEPLOYED site (real Workers, real chains) — no Anvil equivalent by design. |
| UX honest Telegram link + SDK-analytics-off batch 8e (Findings20260711 UX-012/033/043) — "Send a test alert" round-trip gates the linked state on a real signed delivery (`POST /telegram/test`), clearer labelled "unlink this wallet" control, WalletConnect/Coinbase connector telemetry disabled | Live-only (UX-012/043) + build-level (UX-033) | UX-012 needs the deployed agent Worker (`POST /telegram/test` — CORS gate, D1 chat lookup, `test-alert` signature scope) AND the Telegram bot to actually deliver — no Anvil equivalent (same reason as the existing link-flow row). The signature-scoping + chat-lookup logic IS unit-tested in CI: `apps/agent/test/linkAuth.test.ts` (test-alert acceptance + cross-action replay rejection) and `apps/agent/test/db.test.ts` (`getTelegramChatId` returns chat+locale, null on missing/empty row, en-default). UX-033 is a connector-config change verified by the ABSENCE of analytics beacons — a property of the built app, checked live/in devtools, not fork-reproducible. | Post-deploy: on alpha02.vaipakam.com, issue a Telegram link with a dev wallet, complete the bot handshake, click "Send a test alert" → a real message arrives and the card flips to "linked"; a wallet with no completed handshake shows the "couldn't find your chat" message and stays unlinked; the "Unlink this wallet" labelled block is present; DevTools Network shows NO `pulse.walletconnect.org` / Coinbase-metrics beacons on opening the connect modal. Extend `live-alerts-link.mjs` with the test-alert click on next touch (currently the **Gap** row above). |

| Grace-window parity #1235/#1236 (contract #1189) — close-early and refinance stay reachable THROUGH the grace window: preclose card renders in-grace with a late-fee note (quote = `calculateRepaymentAmount`, which already includes the fee) and blocks only strictly past grace; refinance form stays offered in-grace with a past-due banner, the payoff quote/pending-watch figures are time-aware (late fee at chain-now), and the standing approval/reapprove target covers the fee at the request's LAST fillable moment (own expiry vs grace end) so a grace-window accept can't fail on a short allowance; a past-grace pending request reads as dead (funding warnings stop, cancel-to-unwind remains) | CI-Anvil (unit + fork) | `src/contracts/loanLive.test.ts` pins the fee slope/cap/boundaries (`lateFeeAt`), the time-aware payoff, and the approval bound's expiry-vs-grace-end min logic. `tests/21-grace-preclose.spec.ts` builds a loan, warps chain time past maturity (inside grace), and asserts the close-early card + late-fee note AND the refinance form + past-due banner render, then drives the in-grace preclose to Repaid on-chain. NOTE the refinance half of this row is a RENDERING assertion only — that the form is still offered in grace; refinance is driven through acceptance and on-chain settlement by `tests/29-refinance-completion.spec.ts` (#1588), not here. As the suite's first permit-path write AFTER the earlier specs' time warps, it also inherently pins the chain-time Permit2 deadline (a wall-clock deadline signs already-expired permits once chain time runs ahead — the bug this spec exposed). | Post-deploy eyeball on a past-due testnet loan (if one exists): position page shows Close early with the "past its due date" note and Refinance with the same; strictly past grace both disappear and a pending refinance card reads "no lender can accept this request any more". |

| #1247 list windows (PAG-001…006, 008, 012) — every big list renders a 25-row window + "Show N more" (shared `WindowedRowList`/`useVisibleWindow`, the Activity pattern): Positions offers/live/ended groups (attention group whole), Offer Book (screening scoped to the window), Claims, Rent listings, Desk open-orders + positions tabs; ApprovalsCard windows the queried TOKEN set ("Check N more tokens" widens the scan); claimables chain walk gains the fail-loud WALK_CAP 2000; desk book chain path refuses to hydrate a pair bucket >600 (fails over to the capped indexer copy) | CI-Anvil (partial) + live (gap) | Every existing spec exercises the windowed lists below the 25-row page (behaviour unchanged at fixture scale — specs 03/04/17/21 render these surfaces and stay green). The >25-row window + Show-more click, the approvals widen, and the hydrate-cap failover need row-count fixtures the fork tier doesn't build (dozens of offers/loans per wallet); follow-up: seed a 30-row book in a spec on the next e2e touch and assert the 25-row window + Show-more reveal. | Post-deploy eyeball on a position-rich dev wallet: Positions/Claims/Offer Book render 25 rows + an honest "Show N more"; clicking grows by 25; approvals card shows "Check N more tokens" when >25 candidate tokens exist. |

| #1247 indexer caps (PAG-007/009/010/011) — the four remaining unbounded indexer routes capped with truncation flags (`/claimables` 200 newest terminal loans; `/offers/markets` 200 deepest markets; rate-candles newest-10,000-fill scan; signed book per-side `truncated`), and the alpha02-visible piece: the desk's own-signed-orders block reads the book scoped to the connected wallet (`?signer=`) so a maker's off-market order is never hidden behind other makers' top-of-book depth | CI (unit) + live (gap) | `apps/indexer/test/claimables.test.ts` / `offerRoutesMarkets.test.ts` / `rateCandlesRoute.test.ts` / `signedOfferRoutes.test.ts` pin every cap against real migrated SQLite: newest/deepest kept + oldest/shallowest dropped, `truncated` true past the cap and false under it, the signer scope returning a maker order the price cap hides, checksummed-signer normalization, and the `bad-signer` 400. The fork tier serves no signed-book route, so spec 19's signed-book path is unchanged; follow-up: assert the own-orders block on a signer-scoped fixture on the next spec-19 touch. | Post-deploy: on the deployed desk with the dev maker wallet, post a deliberately off-market signed order plus verify the open-orders tab still lists it (fetch scoped by `?signer=` visible in DevTools), and confirm `/offers/markets` + `/signed-offers` responses carry `truncated`. |

| #1023 Activity historical-participant filter — the feed's loan-id participation filter unions the wallet's PERMANENT `/loans/by-participant` history (new `scope=all` route variant: NFT-leg + sale-vehicle + unhealed-stub rows included) on top of the held-position and ingest-lag legs, so actor-null events (LoanSettled, keeper LoanDefaulted) for claimed/transferred positions stay in the feed; participant-leg failure → feed unavailable (never a silently narrower filter); >500 participations folds into the recent-only disclosure | CI (unit) + CI-Anvil (inherent) + live (gap) | `apps/indexer/test/loansByParticipant.test.ts` pins the `scope` param (desk default unchanged, `all` includes the shapes desk hides, `bad-scope` 400). The fork-tier stub already serves `/loans/by-participant` (ignoring `scope` — its data is desk-shaped by construction), so the existing Activity specs inherently run the new three-leg gate. Follow-up: on the next Activity-spec touch, assert an event for a CLOSED loan renders after the position NFT is claimed. | Post-deploy on a dev wallet with a repaid+claimed loan: the loan's settlement event appears in Activity even though the position no longer enumerates on-chain; DevTools shows the `/loans/by-participant?...&scope=all` read. |

| #1268 / E-10 Claim-All CTA — the Claim Center batches every ready payout into ONE `multicall` signature: lender/borrower loan proceeds (`claimAsLender`/`claimAsBorrower`), pending interaction rewards (`claimInteractionRewards`), and free vault VPFI (`withdrawVPFIFromVault`). Per-item include/exclude preview; vault-VPFI opt-IN (off by default — it backs the fee-discount tier); every item `allowFailure=true` (a racing/stale item is skipped, not fatal); selection capped at 30 (MulticallFacet.MAX_MULTICALL_CALLS); per-item outcome surfaced by re-deriving eligibility post-receipt (claimed items drop off, skipped remain); live fail-closed sanctions re-read before submit (the batch can include the rewards claim); default selection capped at the window page so no pre-checked payout is hidden below the fold (Codex #1291 r1); a manual uncheck survives a background refresh (selection is override-reconciled, not reset-to-defaults) while a wallet/chain switch resets the opt-ins (per-wallet vault-VPFI opt-in can't leak); the checklist is windowed; borrower residual labels are status-aware (a defaulted/matched borrower isn't promised "collateral back"). Card renders only once the claimables list AND the rewards/vault-VPFI reads have settled (a still-loading balance never silently drops a leg; a failed read is disclosed), and for ≥2 total payouts; Claims suppresses the "Nothing to claim" empty state when a reward/vault payout is showing | CI (unit) + live (gap) | `src/data/claimAll.test.ts` pins the pure assembly: role→selector mapping + args, status-aware borrower labels (defaulted→surplus, internal_matched→residual, not "collateral back"), rewards/vault appended in order, vault-VPFI default-off + whole-free-balance amount, zero legs omitted, stable unique keys, `defaultSelectedKeys` caps pre-checks at the cap (whale lands submittable), `isLoanItem` leg split, and `encodeClaimAllCalls` decodes back to the right Diamond calls with `allowFailure=true` + the MAX_CLAIM_ALL=30 lock. The fork tier's single-claimable state renders 1 item (card hidden), and a genuine ≥2-item batch needs multi-claimable setup the fork doesn't build; follow-up: on the next spec-04 touch, seed a second claimable (or rewards) and assert the card renders + the batch submits one tx. | Post-deploy on a dev wallet with ≥2 ready payouts (e.g. a repaid loan + pending rewards): the Claim-All card lists both, one signature claims both (DevTools shows a single `multicall` tx), vault-VPFI starts unchecked, and any item skipped by a race remains listed below. |

| #1213 / E-11 notification center (frontend) — a bell + unread count in the connected-app header opens a dropdown of the wallet's newest loan updates, read from the indexer's per-wallet feed (`GET /notifications/:addr`). Rows are outcome-worded per kind (matched / partial repay / repaid / defaulted / internal-match close) with an icon, deep-link to `/positions/:loanId` (which re-verifies on chain), and a loan-id-less row renders as a plain line not a dead link. Read/unread is CLIENT-side: a per-wallet, per-chain last-seen cursor keyed on the feed's chain-order `(block, logIndex)`; opening the panel marks everything loaded as read and clears the badge (no server mark-read → nothing for a stranger to clear); the cleared state survives a reload and never regresses; the badge caps at "9+". The bell shows only when connected; an unreachable indexer states so honestly instead of a fake-empty inbox | CI-Anvil (unit + fork) | `src/lib/notifSeen.test.ts` pins the read-state model against real logic: chain-order comparison (block then logIndex), null-cursor → all unread, strict-newer → unread, a null-key row treated as read (can't inflate the badge), the (chain, wallet-lowercased) scoping, the never-regress guard, and malformed-value tolerance. `tests/22-notifications.spec.ts` drives the full flow on the fork tier: connect → badge reads "2" (deterministic stub feed) → open → two rows render + badge clears → click a row → `/positions/2` navigation + panel closes; a second test proves the cleared state persists across a reload (the per-wallet cursor). The mobile-header overflow guard (spec 20) inherently covers the added 38px bell control | Post-deploy on a dev wallet with recent loan activity: the header bell shows an unread count, opening it lists the loan updates and clears the badge, a row deep-links to the position, and the badge stays clear on reload. Requires the indexer deploy with migration 0038 applied (PR 1) so the feed is populated. |

| #1213 PR 2 notification calendar reminders — the indexer's ingest tick runs a pure-D1 calendar sweep (`calendarNotifications.ts`) that materializes time-derived inbox rows no event can produce: `maturity_7d` / `maturity_1d` (borrower — the acting party) and `grace_entered` (both parties, only while the grace window runs). Covers illiquid loans too (no oracle involved). One-shot per (loan, maturity) — an extension re-arms for the new date; rows are stamped at the sweep's head block so the chain-ordered feed sorts them as current; grace mirrors the contract's zero-bucket default schedule; sale-vehicle/stub rows skipped. The bell renders the three kinds with their own icons + outcome copy (older builds fall back to the generic line) | CI (unit) + CI-Anvil (render) | `apps/indexer/test/calendarNotifications.test.ts` pins the window/dedup logic (T-7d in/out of window, T-7d+T-1d inside the last day, grace to both parties + suppressed past grace end, extension re-arm via maturity-keyed dedup, stub/burned-side skips) and the sweep over the real migrated schema (head-block stamping, idempotent re-ticks, terminal/vehicle/stub exclusion). Spec 22 renders a `maturity_7d` stub row end-to-end: badge counts it, its outcome copy shows in the panel, and it deep-links to `/positions/3`. The sweep itself is indexer-side — the fork tier exercises the RENDERING; the D1 sweep runs in CI unit tests against the migrated schema | Post-deploy on a dev wallet holding a loan due within 7 days: the bell shows the "week from its due date" reminder; a past-due testnet loan inside grace shows the grace reminder to both parties, and none once grace lapses. |

| #1213 PR 2b HF-band inbox rows — the keeper's liquidator pass (which already multicalls `calculateHealthFactor` for every active loan each tick) piggybacks band classification and files borrower inbox rows on DOWNGRADE crossings of the fixed protocol lines (`hf_warn` <1.5, `hf_alert` <1.2, `hf_critical` <1.05): absence-of-state = healthy (first observation inside a band notifies once), recoveries update state silently, a same-day flap is bounded by a day-bucketed dedup key, the recipient is the CURRENT borrower-position holder (burned sides skipped), an un-indexed loan defers (retries next tick, never swallows the crossing), band state is pruned when a loan leaves the active set, and the whole pass is fail-open so it can never delay a liquidation submit. Illiquid loans revert `IlliquidLoanNoRiskMath` in the same multicall and are inherently excluded (the calendar rows are their risk lane). The bell renders the three kinds with their own icons + durable dip-as-of-notice copy | CI (unit) + CI-Anvil (render) | `apps/keeper/test/hfBandNotifications.test.ts` pins the band math (threshold boundaries, uint256.max→healthy clamp) and the crossing semantics over the REAL shared-schema migrations (first-observation mint stamped at the indexer cursor + cron sentinel, downgrade/silent-recovery/re-tick matrix, day-bucket flap bound + next-day re-alert, deferred un-indexed crossing, current-holder resolution + burned-side skip, terminal-loan state prune, no-cursor skip). Spec 22 renders an `hf_alert` stub row end-to-end: badge counts it, its copy shows newest in the panel, and it deep-links to `/positions/3`. The pass itself is keeper-side — the fork tier exercises the RENDERING; the D1 semantics run in CI unit tests | Post-deploy with the keeper enabled and a dev-wallet loan pushed under HF 1.5 (oracle mock): the bell shows the "health dipped below 1.5" warning once, no repeat on the next tick, and a recovery then re-drop the next day re-alerts. |

| SEO indexing policy (SEO/multilang batch) — per-route head metadata maintained by one table (`SeoMeta` in AppShell): public product surfaces (/, borrow, lend, rent, offers, desk, vpfi, nft, help) carry a route-specific title + ~155-char description + production-origin canonical; wallet-gated per-user surfaces (positions, claims, vault, activity, settings, faucet) and NotFound carry `noindex` and no canonical, backed by X-Robots-Tag rules in `public/_headers`; a stale noindex must never linger after a client-side navigation back to a public route. robots.txt + a 9-URL sitemap are generated at prebuild (`scripts/generate-seo.mjs`) | CI-Anvil (partial) | `tests/23-i18n-seo.spec.ts` asserts the head-tag half: home title/description/canonical + no robots meta, /settings noindex + canonical dropped, and the no-linger navigation back to /help. The `_headers` X-Robots-Tag rules and the generated robots.txt/sitemap are deploy-artifact plumbing Cloudflare applies — not observable via the Vite dev server the fork tier drives | Post-deploy: `curl -I https://alpha02.vaipakam.com/positions` shows `X-Robots-Tag: noindex`; `/robots.txt` + `/sitemap.xml` serve the generated files. |

| Display language (SEO/multilang batch; es promoted in #1316) — a Language card in Settings (native-script labels, wave-1 codes en/es/zh/hi/ja) switches the app language: the PREFERENCE persists (localStorage + the `.vaipakam.com` cookie, honoured by the pre-paint bootstrap next visit) and the copy catalog re-resolves through the i18n-aware proxy (`LanguageRemount` remount), while `<html lang>`/`dir` declare the CONTENT language — a TRANSLATED locale (es) renders its localized catalog and flips the attribute; a placeholder locale (bundle still `{}`) renders ENGLISH fallback text (never raw keys, never a crash), so the attribute stays `en` until that locale's translation actually ships, however the preference arrived (picker, sibling-subdomain cookie, navigator) | CI-Anvil (partial) | `tests/23-i18n-seo.spec.ts` drives the picker: exactly the PICKER_VISIBLE set (wave-1 + ta + fr + de + ar + ko = 10; ar additionally asserts the RTL path — `dir="rtl"` on select + after reload, `dir="ltr"` back on en; bumped in the same diff as each promotion); es → chrome re-renders in Spanish (asserted via the picker's own translated aria-label + the localized card heading), `<html lang="es">`, localStorage persistence with picker AND stamp surviving a reload, then back to en; the placeholder leg seeds a `vaipakam_lang=te` cookie (host-scoped on the CI origin, where writeCookie omits Domain — so the cookie-authoritative seed path runs for real; te has no scheduled bundle, keeping the leg valid as further locales promote) and asserts English fallback + honest `lang="en"` + the preference seeded into localStorage, not scrubbed. Only the literal `.vaipakam.com` cross-SUBDOMAIN scope remains live-only | Post-deploy: pick a language on vaipakam.com, load alpha02.vaipakam.com — the choice carries over (cookie leg); with es promoted, verify the live app renders Spanish with `lang="es"`. |

| alpha02 `.ts`-module string extraction (#1329/#1323 lineage) — three surfaces that rendered English regardless of display language because their strings lived in plain `.ts` modules the `.tsx`-only hardcoded-string guardrail can't see, now routed through the `copy.*` catalog: the **Activity feed** row labels (48 event kinds, `lib/activityView.ts` → `copy.activity.labels[kind]`, module keeps an English humanized fallback for an unmapped kind), the **loan-status badges** on every position/history row (`lib/loanState.ts` exposes a stable `labelKey`+`daysRemaining`; the component resolves `copy.loanState`), and the **Claim Center** batch labels (`data/claimAll.ts` takes an injected `labels` arg defaulting to the English source). Plus the alerts-card rollout "notice". Parametrized labels ("Due in N days", "Loan #N — …") stay English — the factory doesn't translate function-valued entries yet (platform limitation, same as every existing `(n)=>…` helper) | CI (unit) | `src/lib/activityView.test.ts` adds a drift guard: every `ACTIVITY_LABELS` kind has a matching `copy.activity.labels` entry and no stale catalog key survives (build fails otherwise). `src/lib/loanState.test.ts` pins the `labelKey` mapping (terminal statuses, the distinct `beingSettled` vs `closed`, overdue/due-today/due-in-N) and `loanStateLabel` resolving through `copySource.loanState`. `src/data/claimAll.test.ts` (existing) covers the label assembly via the English default. The newly-extracted keys are English-only until the locale bundles backfill them (#1323), so there is no per-locale render to assert on the fork tier yet | Post-deploy on a translated locale (e.g. ta) once its bundle backfills these keys: switch the display language and confirm the Activity feed rows, a loan's status badge, and the Claim-All item labels render in that language rather than English. |

| alpha02 interpolated-copy i18n (#1345; #1329/#1323 lineage) — parametrized catalog entries and inline interpolated notices were English-in-every-locale because the i18n factory passed function-valued copy through untranslated and inline `${}` JSX notices never reached the catalog. A `tmpl(...)` primitive expresses each as an i18next `{{var}}` template (locale-aware `_one`/`_other` plurals + `{{n, number}}` formatting) with call sites unchanged; the factory binds+resolves it and the exporter emits it to `en.json`. All ~105 parametrized catalog fns + ~70 inline notices + the contract-hook guard errors were converted. The hardcoded-string guardrail gained a backtick-prose scan that fails CI on a new interpolation-interspersed notice. Signing text (EIP-712 domains, wallet-signed messages) + proper nouns stay English | CI (unit) | `src/i18n/tmpl.test.ts` proves the mechanism end-to-end against a stub Spanish bundle: positional args → named `{{vars}}`, `_one`/`_other` plural selection, and `{{units, number}}` locale formatting all resolve; English falls back to the template when no bundle carries the key. The en-template drift test (`enTemplate.test.ts`) keeps `en.json` in sync, and `check-hardcoded-strings.mjs` (typecheck lane) fails on any un-catalogued backtick sentence. The new keys ship English-first and translate once the locale bundles backfill (#1323) — so there is no per-locale render to assert on the fork tier yet | Post-deploy on a translated locale once its bundle backfills these keys: switch the display language and confirm a parametrized label (e.g. a "Due in N days" badge), a plural count, and a review-receipt line render localized with the value interpolated. |

| Human-readable rate units (user directive 2026-07-22) — the Offer Book's advanced detail line quotes rates as percent ("rate 9%", "rate band 0%–9%") instead of raw basis points ("900 bps", "rate band 0–900 bps"); integer bps divide into at most two decimals so the percent form is lossless. The `rateBand` / new `rateInline` catalog templates carry the wording (every locale bundle's `rateBand` dropped its " bps" suffix since the interpolated values are now pre-formatted percents). Raw bps remain ONLY in the Rate Desk's hover tooltips (exact-value convention) and the desk's explanatory "stored in basis points" note — never as visible row text. The duplicate percent formatters (`bpsToPercentText` in `data/fees`, `bufferPct` in Rent) consolidated onto the shared `formatBpsAsPercent` | CI-Anvil | `tests/13-token-risk-badges.spec.ts` (first book-row test, which already views a 9%-rate offer in advanced mode) asserts the row shows `rate 9%` and contains no visible `bps` text. The rate-band branch uses the same formatter — covered by the same code path | Post-deploy eyeball: an advanced-mode Offer Book row on alpha02.vaipakam.com reads "rate N%" / "rate band A%–B%" on its detail line. |

| Full VPFI tariff opt-in (#1355 / M2 PR-8) — accept/fill review screens offer the acceptor's per-party Full opt-in (live `C*` quote via `quoteCStar`, auto-seeded editable `maxCStar` ceiling, vault-balance sufficiency warning, revert-vs-downgrade choice, dual-fee honesty copy), a standing-offer creator arms theirs post-create from Open Orders (`setOfferCreatorFullTariff`; signed-order makers can't — #1369), and Loan Details shows the stamped per-party modes + absorbed tariffs, a no-refund-on-preclose note, and the entitlement-travels-with-the-position-NFT note on sale surfaces. The whole surface is HIDDEN while `feeEntitlementEnabled` is off (a Full auth presented while dark fails on chain) | CI-Anvil | `tests/24-full-tariff.spec.ts` — three legs on the fork: (1) dark default renders NO opt-in control; (2) admin-impersonated enable + strict Full opt-in with an empty VPFI vault REJECTS the accept (proves the signed `acceptorFull` reaches the contract — the discriminating leg); (3) the same accept with the downgrade box ticked opens the loan and `getFeeEntitlement` reads a stamped non-Full borrower mode with zero tariff absorbed. The kill-switch is restored in a finally | — (the deployed testnet posture is DARK, so the live half of the DoD review is the leg-1 assertion — no opt-in surface renders on alpha02.vaipakam.com; the enabled-state legs are fork-only until the M2 joint cutover arms the feature) |

| Early-repayment options surface (FunctionalSpecs §8) — the borrower loan page gains a "Ways to repay or exit early" chooser card (BOTH modes: names full repay / partial / close-early / obligation handover / offset / refinance with each path's cost implication up front, jump links in Advanced, one explicit "switch to Advanced" CTA in Basic), plus the two previously-unexposed preclose paths as full flows: **Option 2 obligation handover** (`transferObligationViaOffer` — eligibility-mirrored picker over the live book: same assets, amount range covering the outstanding principal, ≥ collateral, term inside the loan's maturity, not own/refinance-tagged/filled; per-candidate cost quote = seconds-precise accrued + lender rate top-up via `transferEconomicsOf`, live re-verified at submit) and **Option 3 offset** (`offsetWithNewOffer` — rate/duration/collateral form seeded from the loan, duration bounded to the remaining term MINUS a 600 s sizing reserve, with the pre-write re-judge demanding only a 60 s submit buffer over the contract's own bound (#1539/#1535 — the contract compares seconds at MINE time while the form computes at READ time, so flooring to whole days leaves zero head-room exactly when the remaining term is a whole multiple of a day; the sizing reserve is meant to be CONSUMED by review + wallet confirmation, which is why the submit re-judge must not demand it back), MUST-SURFACE transfer-lock + funding disclosures, single approval sized `principal + completion bound`; page-owned `useOffsetPending` pending card keyed on the chain's PrecloseOffset lock with completion-funding watch, restore-approval, and cooldown-gated `cancelOffer`). Repay/partial/preclose surfaces interlock against a live offset (warn banners; repay itself never blocked) | CI-Anvil (unit + fork) | `src/contracts/loanLive.test.ts` covers the sibling settlement mirrors this feature extends (`transferEconomicsOf` / `offsetCompletionBoundOf` ride the same pinned primitives). `tests/25-early-repay-options.spec.ts` drives both flows on the fork: (1) Basic-mode chooser renders all paths + the explicit Advanced switch, a direct-seeded matching borrow request lists with the borrower's cost quote, and confirming the handover rewrites `loan.borrower` to the replacement borrower on-chain (loan stays Active); (2) the offset form posts the linked lender offer, the pending card takes over, `positionLock(borrowerTokenId)` reads PrecloseOffset on-chain, and cancel past the 300 s cooldown releases the lock. Follow-up on next touch: an accept-side spec driving `completeOffset` through a counterparty acceptance (needs a third funded wallet accepting the offset offer). | Live review DONE (read half, 2026-08-03) via `live/live-position-observe.mjs` — WATCH-ONLY, no wallet file needed: three REAL active loans on the live Base Sepolia Diamond (ids 7/10/11, borrower `0xC86B…039D`) load their position pages with the chooser rendering and both new paths named (`handover=true offset=true`), and ZERO uncaught errors — in particular no hooks-order crash, which is the check that matters here because that defect is invisible to typecheck, the production build and a preview deploy alike. STILL OWED (write half, needs `TESTNET_WALLETS_FILE`): driving an offset post + cancel and a handover through the wallet-confirm path against the live Diamond — the read drive cannot sign, by construction. |

| Lender-sale listing window (#1503 PR-A) — the listing form gains a mandatory listing-window selector (1/3/7/14/30-day presets; contract bounds [1 h, 30 d], clamped at loan maturity), the expiry + one-day relist-cooldown note, receipts/lock-warning copy naming expiry as a way the listing ends, and window changes voiding a given consent + closing an open review; the standing-approval headroom rationale is re-anchored on the 30-day max window (listings no longer live forever) | CI (unit) + **Gap** (fork) | typecheck + `enTemplate`/placeholder-parity pin the new copy wiring; the defi legacy page's arg-forwarding is pinned in `apps/defi/test/pages/LenderEarlyWithdrawal.test.tsx` (7-day default as the 4th contract arg). No fork spec drives the alpha02 listing form yet — the surface predates this matrix and has never had a driving spec | Commit a listing-flow spec (form → on-chain listing with stamped `expiresAt` → cooldown-refused relist) when PR-H of the #1503 series reworks the lender exit surface — that PR rebuilds this exact form, so the spec lands against its final shape instead of twice. |

| Borrower-side listing-hold surface (#1503 PR-A follow-up) — while a lender-sale listing stands on the borrower's loan, `SaleListingHoldCard` explains the hold (offset + collateral withdrawal held — the on-chain guards; full/partial repay and the direct early close stay open, a partial shrinking what a buyer takes over) and the chooser's offset row is marked held with the why; once the listing ends, the same card grows the permissionless "Free held options" cleanup (`teardownStaleSaleListing`, pause-exempt). A sale a buyer has already ACCEPTED but not completed goes further than a notice: the borrower's settlement paths (full/partial repay, close early, obligation transfer, refinance) are PAUSED with the reason stated up front while add-collateral stays available; every one of those paths also re-probes the chain immediately before it sends, and an unanswered probe pauses rather than proceeds. The pause is scoped to `active` ONLY: `_completeLoanSaleImpl` reverts `LoanNotActive`, so on a `fallback_pending` loan the completion is already stranded and a pause would be permanent rather than momentary — there the borrower gets a non-blocking warning inside the repay review instead, and the live pre-send gate re-reads the loan and refuses only when the chain confirms it is still Active. State is a chain-only teardown-simulation probe (`data/saleListingHold.ts`): success→clearable, `SaleListingLoanStillLive`→live, `NoStaleSaleListing`→none, or →accepted when the lender NFT is still sale-locked, anything else fails CLOSED (renders nothing — incl. the pre-refresh Diamond, gated on a loupe read of the 4-arg listing selector). | CI-Anvil | `src/data/saleListingHold.test.ts` pins all four classifier arms (incl. the sale-locked accepted arm) + the fail-closed default, AND sweeps the whole capability×probe matrix through the extracted `computeHoldGate` — including the invariant that a surface is never left open on an answer the app isn't rendering, and the two regressions that came out of that interaction (an errored re-ask of a known-negative capability must NOT pause; an errored revalidation of a known-positive one must). `tests/26-sale-listing-hold.spec.ts` drives the full lifecycle on the fork (listing → live hold card + held offset row, no cleanup button → warp past expiry → cleanup review + confirm → on-chain link severed); SELF-ARMING via a loupe probe: it skipped while the fork (live Base Sepolia) predated the facet cut that routes the 4-arg listing selector, and armed itself automatically when that refresh landed (2026-08-02) — the selector now resolves to a real facet, so this spec RUNS. The probe stays in place: it is the same positive facet-version signal the in-app surface gates on, so a future chain that lags the checkout skips cleanly instead of failing opaquely. | Live review PARTIAL (2026-08-03) via `live/live-position-observe.mjs` — WATCH-ONLY: the borrower position page renders clean on three real live loans with NO hooks-order crash (the #1511 P1 this surface shipped and then fixed), and the hold card correctly renders NOTHING on all three, which is the right answer — no lender has a sale listing standing on chain right now, so the hold state is not reachable to observe rather than being absent in error. The facet dependency is confirmed live: a loupe probe of the post-refresh Diamond routes `teardownStaleSaleListing` to a real facet, so the in-app capability gate opens. STILL OWED (needs `TESTNET_WALLETS_FILE`): a lender listing on a min window so the hold actually appears, then the post-expiry cleanup click freeing the options on the live Diamond. The accepted-sale PAUSE has no fork arm — reaching it needs a sale acceptance whose completion does not settle in the same transaction, which the post-refresh contracts make atomic; it is a legacy/manual-recovery shape, so the classifier arm is unit-pinned and the paused rendering is left to the live review if such a listing is ever observed. |

| Risk-access page (#671/#728 defi port) — a wallet-gated `/risk-access` page (linked from Settings' More list, noindex in SeoMeta + `_headers`) exposes the vault's two GLOBAL self-sovereign risk controls: the risk level (three tiers, plain-language hints, honest enforcement note — the retail deploy's gate is OFF and the page says "not enforced yet" rather than implying enforcement) and strict mode — where ENABLE is honestly withheld (alpha02 has no surface yet to collect the per-deal mid-tier acknowledgement strict mode demands; enabling would lock the user out of their own mid-tier accepts once enforcement is on — Codex #1517 r1) while DISABLE stays available as the recovery lever (a risk-increasing change under the contract model, bounded by the disable-linger cooldown), with the disable-linger note. Trust rules ported from the defi original's review findings: controls never render over a failed critical tier read; a held-but-not-active level is called cooling vs terms-stale only from trustworthy reads (`classifyHeldTier`), with the in-place re-affirm offered only for stale; failed linger read says "couldn't check"; the advanced detail line calls a cooldown pending only for a FUTURE unlock timestamp (the contract stamps it on every write). Contextual recorders (illiquid pair consent at create, strict-mode mid-tier ack) are a deferred follow-up | CI-Anvil (unit + fork) | `src/data/riskAccess.test.ts` pins the pure classification (cooling vs stale vs unknown — a coerced 0 on a failed anchor/terms read must not flip the comparison; strict-linger predicate matrix). `tests/24-risk-access.spec.ts` drives the real facet on the fork: Settings → page, fresh vault reads Blue-chip active + honest "not enforced yet", raise to Broad liquid (real `setVaultRiskTier` write) re-renders the raised choice, lower back is immediate (✓), and strict mode renders the withheld-enable note when off, then (seeded ON via a direct on-chain write) disables through the UI's real write. Cooldown/terms-bump re-lock rendering is covered at the unit layer — the fork can't bump governance risk-terms | Post-deploy eyeball on alpha02.vaipakam.com: Settings shows the Risk access row; the page renders the connected dev wallet's true tier and the strict-mode toggle round-trips on Base Sepolia. |

| Stuck-token recovery (T-054 defi /recover port) — a deliberately UNLISTED `/recover` page (no nav/Settings entry; noindex in SeoMeta + `_headers`; the ONLY in-app path is the Help page's new `#stuck-tokens` explainer — the dust-poisoning safety gate) returns ERC-20s that landed in the vault outside the deposit flow to the connected wallet. Form (token / declared sender / amount capped at the on-chain `balanceOf(vault) − tracked` surplus) → in-page review step with the standing warning + typed-CONFIRM friction → EIP-712 RecoveryAcknowledgment (live domain/nonce/ackTextHash reads) → `recoverStuckERC20` → outcome decoded from the receipt's events (`StuckERC20Recovered` success vs `VaultBannedFromRecoveryAttempt` ban-as-outcome — the contract deliberately does not revert on the sanctioned path). Post-broadcast honesty rules (Codex #1547 r5): the review card and the Help explainer LINK the Advanced User Guide's stuck-token section the signed declaration makes the user attest to; a wallet REPLACEMENT (speed-up / cancel) is followed rather than lost, so every outcome card and explorer link names the transaction that actually mined, and a replacement carrying no recovery event reads as "nothing was recovered" instead of "the outcome couldn't be read"; and the broadcast-but-unconfirmed card offers a RECONCILE re-read of the stored hash instead of a start-over that would discard it — only a resolved receipt (any outcome) unlocks a fresh form, so a tx that quietly mined can't be recovered a second time over the incremented nonce. Codex #1547 r6 hardened four more: the broadcast is PERSISTED per (chain, account) via the shared pending-marker store, so a reload / account switch / network switch HIDES rather than discards it and coming back rehydrates the pending card instead of a blank form over a live transaction (cleared at every settled landing, so a completed recovery never rehydrates); the reconcile is REPLACEMENT-AWARE — an exact-hash receipt can never resolve for a replaced tx, so a miss falls back to the account's on-chain recovery counter (incremented exactly once per PROCESSED attempt, on both the success and ban arms): counter moved -> terminal "your attempt went through, we can't say which outcome" card linking the stored hash as the SUBMISSION; counter unmoved -> only after a positively-unmined `getTransaction` probe rules out "still queued" does it land the terminal never-processed card with start-over; any read failure stays pending; SMART-CONTRACT accounts are blocked before the form (the facet recovers the signer with ECDSA and requires it to equal msg.sender, so a Safe can only ever revert), probed with `getCode` and failing OPEN so a failed read can't lock out a real EOA; and the availability probe distinguishes a CONFIRMED-unset oracle (permanent, no retry offered) from an UNREACHABLE one (bounded 3x auto-retry + a Retry button) — both still fail closed. Codex #1547 r7 tightened five more: "nothing was recovered" now needs POSITIVE evidence on all three legs — counter unmoved AND a positively-not-found `getTransaction` (an errored/unreadable probe, or a mined-but-receiptless tx, keeps the card PENDING, where before a failed read was treated as proof the tx was gone and cleared the record into a fresh form) AND chain time past the SIGNED DEADLINE now carried in the record, since an unexpired signature can still be re-broadcast and mined; the counter-advanced 'executed' verdict became a PERSISTED terminal lock (`settled:'executed'` on the same per-identity record, rehydrated on mount) with NO plain start-over — a processed attempt may have moved only part of the surplus — whose only exit is a two-step acknowledge-then-reset stating the processed attempt already consumed its signature and that anything after is a separate recovery; the reconcile mutex is generation-keyed like `inFlightRef` (an identity change mid-read can no longer leave the new identity's button dead, nor an old `finally` clear a newer claim); `onReplaced` RETAINS viem's reason, so only 'cancelled'/'replaced' earn the definite nothing-was-recovered copy while a 'repriced' bump — same calldata/destination/value — lands the outcome-UNKNOWN card instead; and the pre-sign oracle re-check no longer collapses a thrown probe into a confirmed zero (throw → 'unreachable' + refilled auto-retry budget + retryable message; zero → 'unset' + the permanent copy; neither may sign). Codex #1547 r8 closed eight more: the pre-sign wallet re-screen now passes `failClosed` so an UNREADABLE `isSanctionedAddress` aborts into the same retryable blocked treatment the availability probe gets (it previously fail-OPENed to "clean" and signed); the `getCode` gate recognises the EIP-7702 delegation designator (`0xef0100…`, case-insensitive) and treats a delegated EOA as an EOA, so only NON-delegated code blocks; the signed attempt is PERSISTED BEFORE `writeContract` is awaited (`txHash: null`), so a lost JSON-RPC reply lands the HASHLESS pending card — no explorer link, its own copy — instead of an armed review, and reconciliation for a hashless record skips every by-hash probe and adjudicates from the recovery counter + signed deadline alone; `pendingMarker.write` now RETURNS success (loan-scoped callers ignore it) and a refused write raises a non-blocking degraded-state warning saying a reload will lose the safety record; every clear is IDENTITY-MATCHED (by hash, or by signed nonce for a hashless record) so a newer record another tab wrote survives; a cross-tab `storage` listener rehydrates a form/review tab onto the other tab's pending/executed card, and — the safety-critical half — the persisted record is RE-READ immediately before signing and an existing attempt refuses the signature; only 'cancelled' now earns the definite nothing-was-recovered copy ('replaced' means only that destination/value/input differ, which a second recovery with other params also satisfies, so it falls to the outcome-unknown card); and a reverted receipt reconciled from a REHYDRATED record lands on a fresh usable form with the reason instead of an empty review card. Codex #1547 r9 closed three more: every write that re-points the persisted record at a transaction hash is now IDENTITY-MATCHED like the clears already were (a wallet that answers late can no longer stamp its hash onto a NEWER record another tab wrote — which the older attempt's later clear would then delete, unprotecting the newer recovery); an EXPLICIT wallet rejection of the transaction (viem `UserRejectedRequestError` / EIP-1193 4001, matched by name + cause-chain walk because a pnpm workspace can resolve more than one viem copy) is the one post-signature failure that positively proves nothing was sent, so it clears this attempt's record and returns to review with the normal rejection message instead of stranding the flow on the pending card until the signed 30-minute deadline expires — ambiguous transport failures keep the pending treatment; and both address fields reject the ZERO address (it passes `isAddress`, so a user could otherwise reach review and sign an ownership declaration naming an address nobody controls), gated on the form with an inline hint and re-asserted immediately before signing. Codex #1547 r10 closed six more: the outstanding-recovery record is now CLAIMED (re-read, then written hashless) BEFORE the signature prompt opens rather than after it returns, and every attempt carries its own `attemptId` — two tabs could otherwise both pass the pre-sign read, and because both attempts share the same signed recovery nonce the loser's later `forgetSignedAttempt` deleted the WINNER's record, the only durable lock over its live transaction; `pendingRecoveryMatches` now answers on `attemptId` when both sides carry one and falls back to the hash/nonce rule only for pre-r10 records, so every clear/update touches only THIS attempt's record; the reservation is RELEASED when the signature is declined/fails (nothing was authorised) and when the identity generation advanced while the prompt was open (checked BEFORE persisting or broadcasting — the flow previously wrote the record and only then hit the stale-generation return, locking the old identity until its 30-minute deadline over a transaction `writeContract` never sent); the receipt-less `settled:'executed'` lock is written through the same matcher (`writePendingRecoveryIfMatches`) instead of unconditionally, so it can't stamp an older attempt's verdict over a newer tab's live record; `decodeRecoveryOutcome` RETAINS the decoded event args (`token`/`amount`/`declaredSource`) and `outcomeStepFrom` builds every success/banned card from THOSE, falling back to the submission context only for what the event doesn't carry — a replaced transaction was previously reported with the ORIGINAL amount/symbol (or the original declared source on a ban) next to an explorer link showing different values — with an event naming a token the record doesn't know rendered as raw base units beside that token's address rather than formatted with this attempt's decimals; the CONFIRMED-but-undecodable card's copy no longer contradicts itself (it told the user "don't sign again / refresh" while rendering Start over over an already-cleared record — it now sends them to the transaction and frames the action as a SEPARATE new recovery, while the unconfirmed variant keeps its genuine don't-sign-again wording); and the `symbol()`/`decimals()` per-read catch classifies only a CONTRACT-LEVEL absence (revert / zero-data, matched by viem error NAME down the cause chain with transport names excluded first, mirroring `isTransactionNotFound`'s multi-viem-copy discipline) as the raw-units token — a transport failure now re-throws into the existing lookup-failed path, where before ANY failure made an ordinary 18-decimal token parse at 0 decimals and a user typing `1` signed for one base unit. Codex #1547 r11 closed three more: the pre-sign reservation is now claimed under a REAL cross-tab mutex (`navigator.locks.request` with `ifAvailable`, lock name per `chainId`+account) held across the read+write pair ONLY — never across the wallet prompt, which would wedge every other tab behind a dialogue the user may leave open — because r10's per-attempt id made a two-tab collision survivable without ever ELECTING a winner (both tabs could read "no record", both write their own id, both sign and broadcast); a lock held by another tab is treated exactly like an existing record (`errAttemptInFlight`, no signature), and browsers without Web Locks fall back to write-then-VERIFY (re-read; a stored id that isn't ours aborts before signing) with the residual write→verify window documented in place; the `getCode` account-type verdict is TAGGED with the address+chainId it was read for and counts only while that tag matches the connected wallet, so an EOA→Safe switch shows the "checking" card instead of rendering the form under the previous account's `eoa` answer while the new probe is in flight; and the Advanced User Guide's own recovery step — the guide the review card links out to — pointed at `/app/recover`, a path that is not a route on the marketing site, so every one of the 10 locale editions now links the connected app's real recovery URL (`https://alpha02.vaipakam.com/recover`, matching the App.tsx route + SeoMeta canonical origin). Codex #1547 r13 closed two more: every TERMINAL outcome card (success / banned / outcome-unknown) is TAGGED with the account+chainId it describes and a mismatched tag is suppressed SYNCHRONOUSLY during render — those cards deliberately render ahead of the account/oracle gates (the r2 rule), so the passive identity reset was one frame too late and a direct account→account (or chain→chain) switch repainted the previous wallet's card with its explorer link built from the NEWLY selected chain's explorer base; the tag now also SOURCES that link, and a suppressed card reads exactly as "no terminal step" (fall through to form/probing) rather than waiting on the effect; and the cross-tab `storage` listener now handles the REMOVAL case — another tab rejecting the signature or reading a definitive receipt deletes the shared record, which used to leave this tab on a meaningless pending card (for a rejected HASHLESS reservation there is no transaction for Check-again to find, so it stayed locked until the 30-minute deadline). A removal releases the card only when the removed record (identified from `event.oldValue` through the same `pendingRecoveryMatches` rule) IS the attempt the card describes and the card is still UNRESOLVED; a newer attempt is left alone and a settled verdict (success/banned/executed-lock) is never cleared. Codex #1547 r14 closed two more: the BANNED outcome card now states the whole consequence — the declared sender being sanctions-listed flags the WALLET and blocks every new-position action (offers, deposits, recovery and the like) while existing loans stay repayable/closable, and the block lifts automatically on de-listing — reusing the standing warning's / Help explainer's r2 wording, because that card deliberately renders AHEAD of the generic sanctioned-wallet gate and is therefore the only post-transaction status the user gets (the old "recovery is locked for your vault" left them to discover the protocol-wide block by hitting it elsewhere); and the `storage` listener now decides from the EVENT's own `oldValue`/`newValue` instead of re-reading the slot — a backgrounded tab whose queued event lands after another tab both removed the displayed attempt AND claimed a new one re-read the NEWER record, took the "slot still occupied" branch, refused to replace its card, and did the same on the following write event, stranding it forever on an attempt that no longer owns the slot; the handler now RELEASES the card whose record `oldValue` names and only THEN adopts `newValue` (release-before-adopt, or the stale card blocks the newer record), with every r13 narrowness intact (pending unknownOutcome cards owned by this identity only, settled verdicts never cleared, a non-matching `oldValue` leaves the card alone, and the missing-`oldValue` whole-storage-clear fallback preserved). Codex #1547 r15 closed three more: the SUCCESS card now carries an explicit "Recover another token" action (the route stays mounted and the record is already forgotten, so a wallet holding a SECOND unsolicited token previously had to reload or navigate away and back — the release fragment already promised a resolved result lets a new recovery begin), while the BANNED card deliberately gets NO such action, since that wallet is blocked protocol-wide until the declared address is de-listed and a fresh form could only lead to another doomed signature; the OPTIONAL metadata reads (`symbol()`/`decimals()`) now take their fallback on ANY non-transport failure — r10's revert/zero-data-only predicate re-threw the `ContractFunctionExecutionError` + `IntegerOutOfRangeError` a legacy **bytes32** `symbol()` (MKR and its pre-standard contemporaries) produces, so a token the node answered perfectly well disabled recovery entirely instead of falling back to the shortened address — with a bytes32 re-read (right-padding trimmed via `hexToString(size:32)`, inside the failed read's own catch so the happy path never pays for it) attempted first so the real ticker still renders, and the transport-name veto kept as the sole discriminator, lifted into `lib/errors.isTransportFailure` so it is unit-testable and shared; and the `storage` handler's step snapshot is now written by `setStep` ITSELF in the same synchronous turn as the state update instead of by a passive effect one render later — another tab that reserves an attempt and immediately removes it (a signature declined the moment the wallet opened) delivers both events in one task, React batches the two updates with no render between them, and the mirrored ref still read `form` on the removal, skipping the release and stranding this tab on the hashless card it had just adopted | CI-Anvil (partial) + not-triggerable (ban, replacement, pending, receipt-less reconcile, deadline-gated verdict, contract wallet, unreachable probe, hashless attempt, 7702 delegate, storage-refusal warning, cross-tab lock, wallet rejection, late-reply clobber, regressed counter read) | `tests/27-recover.spec.ts` drives the real facet: in via the Help explainer link, the FAIL-SAFE blocked state renders first — the spec SETS the oracle to the zero address itself through the admin-impersonation path (Codex #1547 r12) rather than assuming the fork's deployment ships it unset, since a retail deploy is required to configure one post-deploy and a configured-oracle fork would correctly render the form and fail the strict assertion — and with none configured recovery hard-requires it (SanctionsOracleUnavailable), so no form is offered; the spec then installs a benign always-false oracle (anvil_setCode + admin impersonation, reset in finally) and drives the real path — dust minted straight to the borrower's vault (genuine unsolicited surplus), surplus renders from chain, the review card's Advanced User Guide link points at the guide's `stuck-recovery` anchor, CONFIRM arms signing, EIP-712 + submit round-trip, success outcome decoded from the event and the wallet balance delta equals the dust. The BANNED branch stays untestable (needs a sanctions-LISTING oracle, which would poison other specs' Tier-1 writes); it is the same event-decode switch the success path exercises, copy-only beyond it. The REPLACEMENT and PENDING/reconcile branches have no fork trigger either — anvil mines instantly, so a wallet speed-up/cancel and an unreadable receipt cannot be provoked; both reuse the same shared receipt-decode helper the success path proves, and the reconcile action's landings are that helper plus the existing outcome cards. The r6 additions inherit that: the spec's happy path pins the persistence CLEAR (a stale record would rehydrate a pending card and fail the very next run), while the rehydrate itself, the counter-based receipt-less landings, the contract-account block and the unreachable-probe retry have no fork trigger — anvil never leaves a receipt unreadable, the fork wallets are EOAs, and the local RPC never fails a read. The r7 additions get a real arm each where one is reachable: the happy path now RELOADS after the success card and asserts a clean form (pinning "a settled recovery leaves nothing behind" inside one run, where before it only held implicitly via the next run's fresh browser context), and a second spec case seeds a `settled:'executed'` record into localStorage and reloads to drive the persisted lock end-to-end — the locked card rehydrates, no plain Start over exists, the explicit new-recovery action stays hidden until the acknowledge step, and after the two-step release a further reload does not resurrect it (no oracle touched: terminal cards render ahead of the availability gate). The deadline-gated "never processed" verdict, the repriced-vs-cancelled split and the pre-sign unreachable/unset split still have no fork trigger — anvil mines instantly and its RPC never fails a read, so a transaction is never absent-with-a-live-signature, never replaced, and never unprobeable. The r8 additions are covered the same way where a trigger exists and named honestly where none does: the happy path now also proves the new persist-BEFORE-send ordering end-to-end (the record is written hashless, upgraded with the hash, and the identity-matched clear still leaves nothing behind after the success card + reload — a mismatched clear would strand a record and fail the reload assertion), and the seeded-lock case still releases through the identity-matched clear. The HASHLESS pending card needs a wallet that accepts a send and never replies, the 7702 arm needs a delegated fork account, the storage-refusal warning needs a browser that rejects `localStorage.setItem`, and the cross-tab guard needs two live tabs on one wallet — none of which the fork rig provokes; the fail-closed re-screen would need an RPC that fails exactly one `isSanctionedAddress` read mid-flow. All five are live-review items below. The r9 additions: the zero-address gate is form-level and asserted by eye at live review (the fork spec always enters real addresses), while the wallet-rejection branch needs a wallet that DECLINES the transaction prompt — the fork rig auto-signs, so it has no trigger — and the late-reply clobber needs two tabs plus a wallet that stalls its reply; both are live-review items. The r10 additions ride the same two arms where a trigger exists: the happy path now also proves the CLAIM-before-signature ordering end-to-end — the reservation is written before the prompt, upgraded with the hash, and the id-matched clear still leaves nothing behind after the success card + reload, so a reservation that leaked (or an id-mismatched clear that stranded one) would fail the reload assertion AND block the next run's re-sign with errAttemptInFlight — and the event-args rendering is what the success card's amount/symbol assertion now reads (the fork's mined event and the submission agree, so the fallback and the event path are both exercised as one). The reservation-release arms have no fork trigger: the fork wallet auto-signs, so a DECLINED prompt can't be provoked, and an account/chain switch mid-prompt needs a wallet that holds the prompt open. The two-tab clobber the attemptId fixes needs two live tabs on one wallet; the conditional executed-lock write needs a receipt-less reconcile racing a second tab; the replaced-transaction event-args divergence needs a wallet replacement (anvil mines instantly); the undecodable-outcome copy needs a success receipt with no recovery event; and the metadata-vs-transport split needs an RPC that fails exactly one `decimals()` read mid-lookup — all live-review items. The r11 mutex rides the happy path rather than a new arm: Playwright's Chromium DOES implement Web Locks, so the spec exercises the LOCK branch (not the fallback) end-to-end — the reservation is claimed under the lock before the prompt, upgraded with the hash, and released by the successful recovery, which the post-success reload assertion pins (a leaked reservation would rehydrate a pending card AND block the next run's re-sign with errAttemptInFlight). The CONTENDED arm still needs two live tabs on one wallet and the fallback arm needs a browser without `navigator.locks` — neither is fork-provokable, both are live-review items. The tagged account-type verdict has no fork arm either (the fork rig connects one EOA and never switches to a Safe mid-probe); its stale-tag behaviour is a render-time derivation, eyeballed at live review. The guide-link fix is content-only and verified by following the review card's link through to the guide's recovery step. The r12 additions: the oracle-gate arm is now CONFIGURATION-INDEPENDENT (the spec creates the unset state instead of inheriting it, so it passes on a fork whose deployment already has an oracle configured — proven by running it against a deliberately configured fork); the REGRESSED-counter guard (a `recoveryNonce` read BELOW the signed value is treated as an unreadable RPC answer, staying pending and keeping the record, never concluding "never processed") has no fork trigger — anvil serves one consistent state and never returns a stale counter, so it needs a lagging/load-balanced RPC and is a live-review item. The r13 additions ride the existing arms where a trigger exists and are named honestly where none does: the seeded executed-lock arm PINS the terminal tag end-to-end — a rehydrated card is tagged from the record's own identity, so had the tag been missing or wrong the locked card would no longer render after the reload and the arm fails; the happy path's post-success reload proves the same for the settled path. The cross-tab REMOVAL gets its own fork arm: a third spec case seeds an unresolved record, reloads onto the pending card and then delivers the same `storage` removal event a second tab would fire — pinning all three rules in one drive (a removal naming a DIFFERENT attemptId leaves the card alone; one naming THIS attempt returns the tab to a usable form; and a seeded executed-lock survives the same removal untouched). Verified as a real regression pin by disabling the handler and watching exactly that case fail. The r14 additions: the same cross-tab arm gained a FOURTH case — a `storage` event whose `oldValue` names the displayed pending attempt while its `newValue` already carries a DIFFERENT (newer, hashless) record, asserting the tab releases the stale card and adopts the newer one (the hashless card's distinct title makes the swap unambiguous) rather than staying stuck; verified as a genuine pin by reverting to the re-read logic and watching only that case fail. The BANNED card's copy still has no fork trigger (it needs a sanctions-LISTING oracle, which would poison other specs' Tier-1 writes) — it is copy-only beyond the same event-decode switch the success path exercises, and stays a live-review item. The one-frame stale terminal card is the one r13 behaviour with no fork trigger — it needs a WALLET SWITCH between two funded accounts while a card is on screen, and the rig connects one wallet per context and never switches — so it stays a live-review item. The r15 additions: the happy path gained the success-card action assertion (click "Recover another token" → the completed card goes and an EMPTY token field returns, without a reload), and the cross-tab arm gained a FIFTH case that dispatches the write and its removal BACK TO BACK inside one `page.evaluate` with no intervening render wait — the r14 case awaited the first render before dispatching, so it did not cover this ordering; verified as a genuine pin by restoring the passive-effect mirror and watching only that case fail. The bytes32-`symbol()` classification is pinned CHEAPLY as a vitest unit test over `isTransportFailure` (built from REAL viem errors: an `IntegerOutOfRangeError` under a `ContractFunctionExecutionError` is not a transport veto, an `HttpRequestError` nested at any depth is) rather than a fork arm — no legacy bytes32 token is deployed on the fork. The BANNED card's deliberate ABSENCE of a start-over action is copy-shaped on the same untestable branch as the rest of that card | Post-deploy live review is a COMMITTED driver — `live/live-recover.mjs`: Help explainer renders (waited for, since Help is a `React.lazy` route and `domcontentloaded` does not await its chunk) and deep-links to `/recover` BY HREF; no nav or Settings link TARGETS `/recover` (destination-based, so a link relabelled "Stuck tokens" or an icon-only one cannot slip past a name query); the deployed `X-Robots-Tag` is read off the navigation RESPONSE as well as the meta tag, because a JS-less crawler never runs React and the fork/Vite tier cannot see deployment headers at all; the page is polled until it SETTLES on one of its definite postures rather than sampled on a fixed sleep (the availability probe auto-retries 3x, which a 4s sample could race); and the posture is judged against the LIVE oracle — unset (the shipped retail default) must present recovery as unavailable with no form, configured must offer the form, with a sanctions-flagged wallet or an unresolved attempt on record reported as a skip rather than a failure since both correctly withhold it. The read-only guard's own `blockedRequests` and uncaught page errors fail the run, so an attempted signing RPC or backend write cannot pass quietly under otherwise-green checks. The oracle arm is the one the fork cannot check honestly, since the fork spec installs a mock oracle to drive the success path. The guide-link arm now requires the ATTESTED SECTION to exist on the page it lands on, not merely a 200: apps/www runs `not_found_handling: "single-page-application"`, so every unknown path returns 200 with the SPA shell. That strictness immediately caught a real production defect the 200-only check had reported green — `headingComponents` rewrote every guide heading with its text-derived slug and discarded the explicit `id` react-markdown passed down from `remarkInlineAnchorToId`, so `#stuck-recovery.what` did not exist on the rendered page and the link the signed recovery declaration attests to landed users at the top of a very long guide instead of its section (fixed in the same PR; the heading keeps its slug so the in-page TOC is unaffected, and the explicit anchor renders as its own marker immediately before it — locale-independent, since every localized guide carries the same `stuck-recovery.*` markers while the slug is derived from the translated heading). Codex #1561 r2 hardened five more: the Help deep link is now required to resolve to THIS app's own `/recover` (origin-checked against SITE, since a path-shaped test accepts `https://attacker.example/recover` and would report the only safety-gated entry point healthy while sending users off-site), with any off-site recovery link in the explainer failing separately; the Settings scan waits for the page to RENDER before scanning, because `Settings` is a `React.lazy` route whose Suspense fallback has no links at all — so a fixed sleep made a deployment that DID add the forbidden link look clean, and a Settings page that never renders now fails rather than reporting link-free; the separately-opened guide page gets its own console/`pageerror` listeners, since `driver.mjs` wires those to the original page only and the run's closing "uncaught page errors fail the run" claim did not cover it; the explicit-anchor marker carries `.doc-anchor` (block, zero-height, the same 132px/96px `scroll-margin-top` the headings have) because the MARKER is what the browser scrolls to and the pages' scroll-margin rules are written against `h1…h4` only — the link would otherwise land with the named section title hidden behind the fixed navbar; and `remarkInlineAnchorToId` moved out of `UserGuide.tsx` into `lib/`, installed on Overview and Whitepaper too, so the stable-anchor scheme the spec promises for every long-form doc actually applies to all three (only the guides carry such anchors today, so it is a capability fix rather than a content one). Codex #1561 r3 hardened four more: the guide arm now measures WHERE the section lands rather than merely that the marker exists — the marker can stay attached while losing `display:block` or its `scroll-margin-top`, which puts the named heading behind the fixed navbar, i.e. the link "works" and the reader sees the wrong thing (verified on the branch preview at both viewports: H3 at 112px desktop / 144px mobile, against a 72px navbar); the robots value is split into DIRECTIVE TOKENS and matched exactly, since a substring test passes `noindexing, nofollowing` — values containing the directive names but not carrying them — with the same helper used for the header and the meta tag so the two can't be held to different standards; the Settings scan runs in BOTH experience modes (set via the `alpha02.mode` localStorage key + reload), because advanced-only nav items and Settings cards are filtered by `isAdvanced` and a profile sitting in Basic would never render — and therefore never flag — a forbidden link added to an Advanced-only surface; and the wallet-specific postures (`sanctioned`, `pending`) are now judged INDEPENDENTLY of the oracle setting, since a durable unresolved record locks the page ahead of the availability state and is the correct posture whatever the oracle says, where before resetting the oracle under such a profile made a correct page read as a deployment failure. Anchor parity across the ten localized guide editions is guarded separately by `apps/www/scripts/check-guide-anchor-parity.mjs` (wired into apps/www typecheck), after review found the English `Advanced` guide carries one section anchor no translation has — latent rather than shipped-broken, since the `stuck-recovery.*` set this flow depends on is uniform across all ten. Recovering real dust on production stays manual (needs genuinely stuck tokens). |

| Recovery declaration reading aid (#1563) — the EIP-712 declaration the user signs on `/recover` is contract-fixed English (its keccak256 must equal the vault facet's `RECOVERY_ACK_TEXT_HASH`), so the signed bytes cannot be translated. A labelled translation now renders BESIDE the verbatim English, visually subordinate, with the label stating which of the two is authoritative. Gated on the ACTIVE locale's own resource rather than on its language code: 34 locale codes are routable while 10 ship translations, and a translated locale's lazy chunk can fail to load — in both cases `copy.*` hands back the English fallback, and rendering THAT under an "in your language" label would be a false claim made at the moment the user affirms comprehension. No real translation → no aid at all (the whole page is English in that state, so there is no mid-page switch left to soften). | CI (unit) | `apps/alpha02/src/i18n/ownLocaleResource.test.ts` pins the gate over all twelve states: real translation, region tag (`es-MX` → `es` bundle), supported-but-untranslated locale, translated locale with no bundle loaded, bundle present but key absent, empty / whitespace-only value (i18next renders these blank rather than falling back, so they are worse than missing), non-string leaf, English active, an unrecognised tag, and a value byte-identical to the English source — presence is not proof of translation, since neither ingestion path can reject source-identical leaves in general (`Vaipakam`, `GTC`, `{{amount}} {{symbol}}` are legitimately identical everywhere), verified by feeding `merge-patch` a Spanish patch echoing the English declaration back and watching it write cleanly. The label is checked the same way and rendered from its own checked value: an English label over translated text states the claim in a language the reader may not read, and a translated label over English text states something false, so both halves are required. Rendering is a truthiness gate over the two values, making the unit boundary the whole behaviour. The aid's ENGLISH source is no longer a second copy of the declaration — page and catalog import one definition (`src/lib/recoveryAck.ts`), so they cannot drift; and because the nine translations cannot be derived from it, `check-locale-coverage.ts` pins the declaration's SHA-256, failing the build if it changes until every locale is re-authored. Verified by mutating the pin: the guard names the new hash and refuses to pass. A build-time gate rather than a runtime one on purpose — a stale reading aid cannot ship at all, instead of shipping with the aid silently suppressed | Post-deploy, switch the app to Tamil on `/recover`: the dashed translated block appears under the English declaration; switching to a language with no bundle (e.g. Portuguese) shows the English declaration alone, with no aid and no label. |
| Nine-locale delivery of `/recover` on the deployed build (#1560 follow-up) — the last two untranslated keys (`copy.match.riskGateCreatorBlocked`, `copy.match.riskGateTierTooLow`) are filled, so `untranslated-baseline.json` is now EMPTY and every advertised language covers `en.json` with no recorded exemption. | Live-only | `live/live-recover-locales.mjs` — TWO independent layers per locale, because either alone can lie. **Resource:** captures the locale CHUNK the deployed page actually fetched and asserts it carries this revision's expected values, including the two keys this change adds. **Render:** asserts the page switched — `<html lang>`, the expected direction (`rtl` for Arabic, `ltr` for every other locale, so a regression stamping RTL on German fails), and an `<h1>` equal to that locale's own `copy.recover.title` rather than merely "not English", so a loader wired to the wrong bundle or a redirect to another localized page fails too. Every expected string is read from the repo's locale JSON — no second copy to drift. Fresh profile per locale, removed via the driver's `done()`. Keyless (`launch({ keyless: true })`): no wallet injected, no dev-wallet key read. **Verified both directions** — green on the branch preview built from this commit; the SAME drive run against production (which predates the change) fails all nine with `deployed chunk is missing the expected copy.match.riskGate*` and exits non-zero, while its render layer passes there, so it is the resource layer doing that work. | The coverage guard reads the JSON on disk and can only prove the KEYS EXIST; it cannot prove the deployed app ever SERVES that bundle. Locale resources are code-split and fetched at runtime, so a 404ing chunk, a locale dropped from the split, or a detector that never switches all leave the guard green while the reader sees English — and because `copy.*` resolves through `i18n.t(key, { defaultValue: englishSource })`, that fallback renders with no error logged anywhere. The fork/Vite tier cannot close the gap either: it serves assets from a dev server with a different chunking story than the deployed Worker. |

| Partial repayment (#1587) — the borrower's chooser offers `partial` as a first-class row on a loan whose offer set `allowsPartialRepay`; the advanced-mode `#partial-repay-card` takes an amount, and `repayPartial` pulls that amount PLUS the interest accrued to now in one transferFrom set, decrementing `loan.principal` by the typed figure alone and leaving the loan Active. The surface fails closed behind a live offset (the linked offer escrowed the current principal) and an accepted-but-uncompleted sale — both answered by the chain, so both hold from any device. It ALSO holds behind a standing refinance request (frozen at the current principal — a paydown would make it permanently unacceptable), but that one only for a request remembered on the CURRENT device: `refinancePending` learns of requests from a device-local `localStorage` marker and `repayPartial` has no on-chain refinance guard, so a borrower acting from a second device is not held. Recorded as an open divergence in `_CodeVsDocsAudit.md` | CI-Anvil | `tests/28-partial-repay.spec.ts` seeds a partial-enabled lender offer (the offer FORM defaults the flag OFF, and offer creation is already covered by `02-post-offer`, so seeding keeps the spec pointed at the gap), accepts it, then drives the card and asserts on-chain that the principal fell by EXACTLY the typed amount and the loan is still Active. Both halves are load-bearing: a full repay also reduces what is owed, and a no-op also leaves the loan open. The chosen amount is bounded against the contract's own live `minPartialBps` floor and the `PartialWouldRetireFullPrincipal` ceiling, so a risk-param change fails in the spec naming the cause instead of as an opaque revert behind the wallet confirm. Mutation-verified: halving the amount the app sends leaves the success banner intact and is caught only by the on-chain equality | — |

| Refinance completion (#1588) — the borrower posts a refinance-tagged borrow request for exactly the old loan's principal with the old collateral's identity repeated; the moment any lender accepts, ONE transaction opens the new loan, pays the old lender principal + mode-aware settlement interest from the borrower's standing approval (the same `settlementInterestNet` the preclose path uses — full-term where the loan sets `useFullTermInterest`, accrued-only pro-rata where it does not, plus any late fee in grace; the loan this spec builds is accrued-only), closes the old loan as Repaid, and re-tags the collateral lien old→new without it ever unlocking. `RefinanceFlow` is FORM ONLY — the live request's standing surface is the page-owned `RefinancePendingCard` | CI-Anvil | `tests/29-refinance-completion.spec.ts` drives the form, then has a SECOND lender accept the request, and asserts the old loan reaches Repaid while the replacement opens Active under the same borrower at the same principal. Supersedes the render-only assertion in `21-grace-preclose` (which proves the card appears in grace, not that the flow works). Two things this spec learned by mutation rather than reasoning: the "posted" line is transient — the pending card takes over immediately, so the durable `Refinance request #N is live` is what proves the request survived its own submit, and its id is cross-checked against the chain's `refinanceTargetLoanId` so an inherited stranger offer cannot stand in; and collateral `asset` + `amount` equality does NOT prove carry-over, because the documented failure mode (creator ≠ loan-init borrower) produces a FRESH pledge of the same asset in the same amount pulled from the poster's wallet. Proof is therefore TWO assertions, neither sufficient alone: the persisted `refinanceCarryOver` flag read off the chain request pins the PATH (the contract's legacy non-carry-over branch returns the old collateral at acceptance, so a re-pledge nets to zero across any window), and whole-flow balance invariance — sampled before posting, compared after completion — pins its EFFECT. The balance assertion was verified to catch a one-wei perturbation; the flag assertion guards a path not reachable through this surface today (forcing it fails earlier on a collateral allowance), so it is a regression guard rather than a mutation-isolated one. The terms typed into the form (ceiling, length) are read back off the chain request and off the replacement loan, so a form that stopped persisting either would fail here instead of posting valid-but-stale terms | — |

**When adding a row**: prefer CI-Anvil. If claiming live-only, write
the one-sentence reason — "easier" is not a reason. If a live-only
feature later grows an Anvil-fakeable core (as the alerts CARD's
fail-closed rendering could), split the row.
