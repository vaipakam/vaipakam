{
  "name": "@vaipakam/agent",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Vaipakam agent Cloudflare Worker (Stage 3 PR4 of the Worker split — see docs/DesignsAndPlans/Stage3WorkerSplitPlan.md). Owns proactive notifications (periodic interest pre-notify, push + telegram dispatchers), public Farcaster Frame at /frames/active-loans, operator services (server-side aggregator quote proxies at /quote/{0x,1inch}), the Telegram bot webhook (/tg/webhook), the diagnostics record capture endpoint (/diag/record), and the frontend-facing settings endpoints (/thresholds PUT + /link/telegram POST). Holds aggregator API keys + Push channel signer + Telegram bot token. Holds NO on-chain transaction key (KEEPER_PRIVATE_KEY moved to apps/keeper), so it cannot move funds. It does hold PUSH_CHANNEL_PK, a real Ethereum key used to sign Push notifications. The older 'a compromised agent produces stale data' claim is withdrawn: this Worker deletes diagnostics/support records, writes loans, notifies real users, publishes listings, and shares a database-scoped D1 binding with the signing Worker (see #1722).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "pnpm run typecheck",
    "typecheck": "tsc -p . --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@pushprotocol/restapi": "^0.0.1",
    "@vaipakam/contracts": "workspace:*",
    "@vaipakam/lib": "workspace:*",
    "ethers": "^6.13.0",
    "viem": "^2.48.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^6.0.0",
    "vitest": "^4.1.4",
    "wrangler": "^4.0.0"
  }
}
